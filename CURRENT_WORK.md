# Current and Next Work

**Purpose:** Single source of truth for task status. Completed items stay here, struck through with a date — nothing moves to another file.

**Last updated:** 2026-06-28 (maintainability pass chunk 10 **DONE** — squashed 139 migration files into 6 across 5 apps: diary 0009-0035 → `0009_v2` and 0036-0090 → `0036_v2` (folds 15 data migrations incl. `seed_singleton`, the TMDB→OMDb cycle, and the 0029/0060/0061/0062 MySQL IF-NOT-EXISTS column guards; caught and fixed an optimizer pitfall where 0086+0087's `AddField(default='programmer')`+`AlterField(default='volunteer')` merged into a single AddField that would have loosened diary-edit-list access — split back to preserve the seeded 'programmer' value), members 0009-0028 → `0009_v2` (folds `populate_status_from_active` + `split_dir_share_name`, `atomic=False` for MariaDB), labs 0001-0027 → `0001_initial_v2` + `0015_v2` (split at 0014/0015 to break a cross-app cycle with members.0014), inductions 0001-0009 → `0001_initial_v2`. mailer+index left alone (single follow-on files). Suite green at 959; black clean. Next: chunk 11 — docs cleanup.)

**Current phase:** Phase 4 ongoing
**See also:** [ROADMAP.md](docs/ROADMAP.md) (wave-by-wave sequencing) · [TASKS.md](docs/TASKS.md) (design rationale & feature specs)

---

## Immediate blockers (fix first)

| # | Bug | Notes |
|---|-----|-------|
| ~~**Bug AE**~~ | ~~**Homeserver: star/shadow mark icons missing on rota; panopticon [e]/tap-to-toggle not working**~~ | ✅ 2026-05-23 — Resolved on homeserver. |
| ~~**Bug AH**~~ | ~~**Star/shadow icons missing in dev — `volunteer*` accounts had no Volunteer record**~~ | ✅ 2026-05-29 — `configure_toolkit_users` now creates `Member` + `Volunteer` records for each `volunteer*` account. Re-run the command to apply to existing dev databases. |
| ~~**Bug AI**~~ | ~~**Missing migration: `ConsumableItem.category` choices added "Cinema Snacks" without a migration**~~ | ✅ 2026-05-29 — Migration `labs/0016_alter_consumableitem_category` generated and applied. |
| ~~**Bug AJ**~~ | ~~**New volunteers got unusable password with no way to log in themselves**~~ | ✅ 2026-05-29 — `_send_password_set_email()` helper extracted; called automatically on new volunteer creation with a welcome message + password-set link (3-day validity). |
| ~~**Bug AK**~~ | ~~**Password reset URL regex rejected Django 5 SHA-256 tokens (token too long)**~~ | ✅ 2026-05-29 — `password_reset_confirm` regex widened; "valid for 24 hours" message corrected to use `PASSWORD_RESET_TIMEOUT` (default 3 days). |
| ~~**Bug AL**~~ | ~~**3 pre-existing diary test failures**~~ | ✅ 2026-06-01 — `test_panopticon_can_save_changes`: POST data missing fields added since the test was written (`calendar_slot_min_hour`, `general_training_enabled`, `volunteer_digest_day`, `lost_and_found_retain_days`, etc.); updated. Also fixed `volunteer_views.py`: `from datetime import timezone` shadowed by `from django.utils import timezone`; replaced `datetime.now(tz=timezone.utc)` with `timezone.now()`. |
| ~~**Bug AF**~~ | ~~**Message banners: duplicate tick/cross (list-style-image + ::before pseudo-element)**~~ | ✅ 2026-05-25 — Removed `list-style-image` from `li.success` and `li.error` in `messages.css`; `::before` rules kept. |
| ~~**Bug AG**~~ | ~~**Batch showings crash on past dates (IntegrityError from Showing.save guard)**~~ | ✅ 2026-05-25 — `Showing.save()` now only enforces the past-start guard when `self.pk is not None` (existing records). `BatchAddShowingsForm.clean()` rejects past date+time combinations with a clear error. "Create as confirmed" checkbox added to form; form validates that event has terms set before allowing it. "Confirm all (N)" button added to event hub (shown when there are unconfirmed future showings and terms are OK). |
| ~~**Bug W**~~ | ~~**Burger menu vanishes on very narrow mobile views**~~ | ✅ 2026-04-28 — Root cause: `.showing-meta` in `edit_rota.html` had `flex-wrap: nowrap`. On multi-room events with duration + room badge + outside-hire badge + link, the combined min-content width exceeds the ~274px available at 320px viewport, causing horizontal overflow. On iOS Safari, horizontal overflow causes the fixed navbar to appear to scroll off-screen. Fix: changed to `flex-wrap: wrap`. |
| ~~**Bug X**~~ | ~~**3-day calendar view button renders blank**~~ | ✅ 2026-05-05 — `buttonText` and `buttonHint` moved inside the `threeDay` view definition in `calendar_index.js`; top-level `buttonText`/`buttonHints` objects removed (unreliable with FC6 scheduler bundle). **Regressed and re-fixed 2026-05-08:** the `views` config object was being defined once with `threeDay`, then reassigned a few lines later to `{ timeGridWeek: {...} }`, wiping the `threeDay` definition. Changed reassignment to a merge (`calendarOpts.views.timeGridWeek = {...}`). |
| ~~**Bug AA**~~ | ~~**Sidebar clips programme/event content on wide screens**~~ | ✅ 2026-05-08 — `layout.css` widened sidebar from 170px → 192px at the 1600px breakpoint, but `site_custom.css`'s `.grid { margin-left: 170px }` content-offset was hard-coded with no matching breakpoint. Fixed by promoting sidebar width to a CSS custom property (`--sidebar-width`) defined once on `:root` with breakpoint overrides; sidebar geometry and `.grid` margin-left now both consume it, so they cannot drift. |
| ~~**Bug AB**~~ | ~~**Event detail page: poster image hidden behind sidebar**~~ | ✅ 2026-05-08 — `event.css`'s 1000px breakpoint had `.grid { margin: 0px 5px 48px }` — a 4-arg shorthand that silently reset `margin-left` to 5px, clobbering layout.css's `var(--sidebar-width)` offset. Content sat at viewport-left + 5px and the left ~50% of the poster was painted over by the fixed nav. Fixed by splitting the shorthand into `margin-top` / `margin-bottom` so layout.css's margin-left wins. Same shorthand trap also lurked in `static_pages.css` (article pages); fixed there too. |
| ~~**Bug AC**~~ | ~~**Event detail page: white card background instead of pink**~~ | ✅ 2026-05-08 — Latent regression from commit `d6310ff5` (Feb 2026): the SnS pink override on `.grid-item` lived in `event_custom.css` (which used to load *after* `event.css` and win), but that commit moved it into `site_custom.css` which loads *before* event.css, so event.css's `background-color: #fff` started winning. Unnoticed for months because the event detail page is rarely viewed in dev. Fixed at source: `event.css` now sets `.grid-item { background-color: transparent }` to match the rest of SnS. |

---

## Maintainability pass (`chore/maintainability-pass`)

Tracking file: [MAINTAINABILITY_PASS.md](MAINTAINABILITY_PASS.md). Chunks 1-5 done 2026-06-26.

| Chunk | Status | Notes |
|---|---|---|
| 1 Delete attic | ✅ 2026-06-26 | ~800 LOC removed. |
| 2 Delete committed creds | ✅ 2026-06-26 | ~700 LOC removed. |
| 3 Latent bugs | ✅ 2026-06-26 | 5 fixes (legacy redirect, calendar summary, dev settings typo, etc.). |
| 4 Shared helpers | ✅ 2026-06-26 | `password_emails`, `_render_admin_email`, `_user_volunteer`, shared test fixture. |
| 5 Split `volunteer_views.py` | ✅ 2026-06-26 | `members/views/` subpackage (8 modules). |
| 7 Split `diary/models.py` | ✅ 2026-06-27 | `models/` package: `site_config`, `event`, `showing`, `rota`, `misc`. Cross-module FKs use string refs `"diary.X"`; `__init__.py` re-exports for back-compat. No schema change; migrations unaffected. Suite green at 959; black clean. `docs/SPEC.md` §8 notes the new layout. |
| 6 Split `labs/views.py` + admin | ✅ 2026-06-27 | `views/` package (10 feature submodules + `_common` + `__init__`). Each submodule carries the original import header verbatim (`.models`→`.​.models` since package is one dir deeper); the one cross-module helper `_user_volunteer` lives in `_common`; loft zone constants + `_serialize_loft_items` live in `loft.py` and are imported by `floorplan.py`. `__init__.py` re-exports every public view name + the three externally-referenced private names (`_user_volunteer`, `_unread_bulletins_for`, `_active_bulletins_qs`) so `labs/urls.py` and `toolkit/index/views.py` keep working unchanged. Source `views.py` deleted. Registered 7 previously-unregistered `labs` models (`RoomNote`, `LoftItem`, `BulletinRead`, `ProcurementPledge`, `CollectiveLink`, `AreaPhoto`, `LoftItemPhoto`) and all 4 `inductions` models in admin. Suite green at 959; black clean. |
| 7 Split `diary/edit_views.py` | ✅ 2026-06-27 | `edit_views/` package (12 submodules): `_common`, `diary_overview`, `events`, `showings`, `templates`, `tags_roles_rooms`, `rota`, `misc`, `site_config`, `film`, `reports`, `__init__`. Each submodule carries the original import header verbatim; the two genuinely cross-module helpers (`_get_omdb_api_key`, `_film_json`) live in `_common`. `__init__.py` re-exports all 53 public names so `urls.py` and the one test importing `_export_template_json` keep working unchanged. Suite green at 959; black clean. |
| 7 Dedup `_safe_json` | ✅ 2026-06-27 | Promoted to public `safe_json(data)` in `toolkit/diary/daterange.py` (the existing shared module) with the `_JSON_SCRIPT_ESCAPES` table. `public_views.py` and `edit_views/events.py` drop their local copies and import it; the now-unused `import json` was removed from `public_views.py`. `edit_views/__init__.py` drops the `_safe_json` re-export. Suite green at 959; black clean. |
| 8 SiteConfig auto-form | ✅ 2026-06-27 | Parity test (`SiteConfigurationConsistencyTests`) was already in place from earlier work — it now uses `base_fields` rather than `Meta.fields`. `SiteConfigurationForm.Meta` switched from an 80-line explicit `fields` tuple to `exclude = ("id",)`, so the form auto-derives from the model `_meta`. New single source of truth `SITE_CONFIG_FIELD_GROUPS` (insertion-ordered dict in `toolkit/diary/models/site_config.py`, re-exported via `toolkit.diary.models`) feeds the edit page's section grouping; the view's 150-line inline `field_groups` list is now one line. `perm_*` fields stay out of the dict (still rendered via `permission_rows`). Suite green at 959; black clean. |
| 9 Feature-flag reconciliation | ✅ 2026-06-27 | Documented the precedence rule in the `SiteConfiguration` docstring (DB row is source of truth; settings.py seeds the row). Added one-line comments at every settings-only flag reader (`TAGS_WITHOUT_TERMS` × 2 sites, `EDIT_INDEX_DEFAULT_DAYS_AHEAD` × 1, `MULTIROOM_ENABLED` × 3 files incl. a module-level note in diary_overview.py since it's read at 5 sites, `MEMBERSHIP_EXPIRY_ENABLED` × 1) marking them as having no SiteConfiguration counterpart. Rescoped to document-comment per chunk plan; per-flag DB-first migration deferred. Suite green at 959; black clean. |
| 10 Migration squash | ✅ 2026-06-28 | Squashed 139 migration files → 6 across 5 apps. diary: 81 → 2 (`0009_v2` 0009-0035, `0036_v2` 0036-0090, folds 15 data migrations incl. `seed_singleton`, TMDB→OMDb cycle, MySQL column guards; fixed an AddField+AlterField optimizer merge that would have loosened diary-edit-list access from Programmer+ to all volunteers). members: 20 → 1 (`0009_v2`, folds `populate_status_from_active` + `split_dir_share_name`, `atomic=False`). labs: 27 → 2 (split at 0014/0015 to break a cross-app cycle with `members.0014` ← `labs.0009`). inductions: 9 → 1 (leaf app, no data migrations). mailer+index left alone. Suite green at 959; black clean. |
| 11 Docs cleanup | pending | CURRENT_WORK archive, split events-and-rota spec, move rewrite essay out of SPEC, fix BRANCH_NOTES link, docs/ARCHIVE index, flatten ROADMAP. |

---

## Phase 1: Next prioritized work (in order)

### 1. S+S feature porting (from `s+s` branch)

| Feature | Status | Notes |
|---------|--------|-------|
| ✅ `Volunteer.user` OneToOneField | Done 2026-02 | Each volunteer linked to Django `User` |
| ✅ Django admin integration | Done 2026-02 | Enabled in `INSTALLED_APPS` + `settings_ss.py` |
| ✅ Programmer permission group | Done 2026-03-01 | `UserForm` gains a `programmer` BooleanField that syncs `Programmers` group membership on save. "Programmer" rota role removed from `ROLES` and event templates — it was a separate thing that caused confusion. |
| ✅ Rota role count limit increase (8 → 30) | Done 2026-02 | `MAX_COUNT_PER_ROLE` overridden in settings_ss.py; all enforcement points auto-parameterized |
| ✅ `SHOW_ARCHIVE_IMAGES` / `IMAGES_START_DATE` | Done 2026-02-28 | Hide event images before configurable date; see TASKS.md 9.27 for rationale |
| ✅ `Showing.rota_notes` field size | Done 2026-02-28 | Extended to 4096; migration `diary/0010` |
| ✅ `Member.email` mandatory | Done 2026-02-28 | `blank=False`; migration `members/0010`; 10 tests updated |
| ✅ Panopticon user management in volunteer edit | Done 2026-02-28 | `UserForm` (username/is_superuser/programmer) shown when `VENUE.show_user_management=True`; gated so Cube tests unaffected. (The `is_active` "Login enabled" checkbox was later removed — login is now driven by `Volunteer.status`; see the Suspension entry in Done.) |
| ✅ Custom Django admin `ModelAdmin` classes | Done 2026-02-28 | `members/admin.py` (Member, Volunteer, User+VolunteerInline); `diary/admin.py` (Room, Role, EventTag, Event, Showing) |
| ✅ Expired members view | Done 2026-02-28 | `/members/expired/` endpoint; reuses `search_members_results.html` |
| ❌ `view_diary_json` endpoint | Shelved | Experimental; existed on `s+s`. Not currently needed. |
| ❌ Legacy URL redirects | Shelved | Old website had different URL structure. Requirements unclear. |
| ❌ `utils/mailoutomatic.py` | Shelved | Standalone mailout scheduler. Can revisit if needed. |

### 2. Template comparison & alignment

✅ **Done 2026-03-01** — All three S+S templates are ahead of `s+s` branch; nothing missing:

- `base_public.html` — IE8 cruft removed, Font Awesome updated, jQuery local, logo CSS fix, DEV watermark
- `view_event.html` — `site_custom.css` added, alt text bug fixed (`showing.event.name` → `event.name`)
- `view_showing_index.html` — `site_custom.css` added, RSS link removed, volunteer notice banner, `showing-internal` class, lock-icon badges on private events, `event_detail_url` routes volunteers to rota

### 3. Open bugs

| ID | Bug | Size |
| -- | --- | ---- |
| ~~**E**~~ | ~~Volunteer login dropdown inaccessible on touch~~ | ✅ 2026-03-02 — sidebar scrollbar fix resolved this |
| ~~**M**~~ | ~~Calendar edit view: simultaneous events overwrite each other on busy days~~ | ✅ 2026-04-04 — `Showing.end_time` now returns `start + 2h` when `event.duration` is None; FullCalendar can detect overlaps and tile events side-by-side |
| ~~**R**~~ | ~~Mobile: public-site sidebar always visible through content (not gated by hamburger)~~ | ✅ 2026-03-03 — `site_custom.css`: sidebar starts at `left: -240px; z-index:200` on mobile; desktop-only `@media (1000px+)` block keeps it always visible; `site-common.js` now also toggles `.sidebar-open` on `#sidebar` |
| ~~**S**~~ | ~~Login page: nav bar overlaps form on mobile; style inconsistent with post-login admin nav~~ | ✅ 2026-03-03 — `star_and_shadow_templates/base_login.html` created: minimal logo-only base (no nav); login, logout, all four password-reset templates now extend it instead of `base_public.html` |
| ~~**T**~~ | ~~Admin toolkit: navbar toggler off-screen on mobile; body padding too large; rota controls overflow~~ | ✅ 2026-03-03 — `base_admin.html`: brand img `max-width:120px`; `padding: 3.5rem 0.5rem` on ≤576px; `.navbar-collapse { background-color: inherit }` on ≤767px; rota controls: date inputs `6em` on ≤640px |
| ~~**U**~~ | ~~Rota: excessive left indentation on `ul.rota` and `.showing_rota_notes`~~ | ✅ 2026-03-03 — `edit_rota.html`: `@media (max-width: 640px)` removes `ul.rota` padding; reduces `.showing_rota_notes`/`.event-links` margin to `0.5em` |
| ~~**V**~~ | ~~Tags/Roles admin: flex/table content overflows horizontally on narrow screens~~ | ✅ 2026-03-03 — `edit_event_tags.html`: `overflow-x: auto` on card-body; `form_edit_roles.html`: table wrapped in `<div style="overflow-x: auto">` |
| ~~**O**~~ | ~~Volunteer rota: event-name links bounce to login (should go to public programme)~~ | ✅ 2026-03-03 — template conditional: programmers/superusers → edit hub, volunteers → public showing view |
| ~~**P**~~ | ~~EventTags page: pre-Bootstrap styling + confusing Promoted/drag UX~~ | ✅ 2026-03-03 — Bootstrap 4 card layout; drag handle hidden on non-promoted tags; footer help text explains In menu / drag model |
| ~~**Q**~~ | ~~Roles page: pre-Bootstrap styling~~ | ✅ 2026-03-03 — Bootstrap 4 card + table layout; read-only badge; yellow new-row highlight |
| ~~**N**~~ | ~~Nav "Rota" link pointed at public `/diary/rota/` instead of edit `/diary/edit/rota/`~~ | ✅ 2026-03-02 |
| ~~**AM**~~ | ~~`/diary/copy` index shows "Closed for private event." for every private/hidden showing~~ | ✅ 2026-06-10 / 2026-06-20 — Fix initially applied to `view_copy_summary.html` only; second fix applied to `view_copy.html` (the actual `/diary/copy` template). Private/hidden showings now suppressed from the index in both templates. |
| ~~**Bug AN**~~ | ~~Nav dropdown: hover highlight wider than item text~~ | ✅ 2026-06-10 — `base_admin.html`: `.navbar .dropdown-menu .dropdown-item { width: fit-content }` constrains the hover highlight to the item's text width. |

---

## Phase 2: Quick wins (independent — start any)

| # | Feature | Size | Notes |
|----|---------|------|-------|
| ~~**8.1 MVP**~~ | ~~**RotaEntry → Volunteer FK**~~ | ✅ 2026-05-12 | `RotaEntry.volunteer` nullable FK (`SET_NULL`) + migration `diary/0028`. Non-superuser rota sign-ups coerced: FK set to `request.user.volunteer`, name written from `member.name`. Superusers keep free text (FK = None). Pronouns tooltip uses FK directly when set; dict fallback for legacy entries. Display in `edit_rota.html` + `view_rota.html` prefers FK name. GDPR anonymisation: FK path primary (`filter(volunteer=v)`), text-match fallback for legacy entries. No backfill migration — production name-variant mapping deferred. 7 new tests. |
| ~~8.13~~ | ~~`IndexLink.description` field~~ | ✅ 2026-02 | `TextField` added; migration `0003` |
| ~~8.14~~ | ~~Volunteer table sort (in-place JS)~~ | ✅ 2026-02 | Click headers to sort without page reload |
| ~~8.15~~ | ~~jQuery UI 1.11 → 1.13.3~~ | ✅ 2026-02 | Drop-in update |
| ~~8.15~~ | ~~Remove Respond.js and IE8 blocks~~ | ✅ 2026-02 | Deleted polyfill and conditional comment blocks |
| ~~8.15~~ | ~~CKEditor 4 → Quill 2~~ | ✅ 2026-03-03 | Quill 2.0.3 vendored; `HtmlTextarea` widget + `htmltextarea.html` rewritten; HTML source toggle added; `&nbsp;` entity stripping on submit; CKEditor directory deleted (1.5 MB removed) |
| ~~8.15~~ | ~~Chosen → native `<select multiple>`~~ | ✅ 2026-03-03 | `ChosenSelectMultiple` stripped to plain `SelectMultiple`; Chosen static files + template deleted; `volunteer_training_report.html` updated; dead import in `edit_views.py` removed; `TagPillSelect` widget added — flex-wrap row of Bootstrap-radius badge toggles for tag fields, pure CSS (`input:checked + span`), no JS; project-level crispy-bootstrap4 template overrides added (`templates/bootstrap4/layout/checkboxselectmultiple.html` + `radioselect.html`) to fix Django's `CheckboxSelectMultiple → RadioSelect` inheritance causing crispy to double-render both widget templates for the same field |
| ~~8.15~~ | ~~wysihtml5.css deleted~~ | ✅ 2026-03-03 | Confirmed unreferenced; file removed |
| ~~8.15~~ | ~~jQuery 2.1.3 → 3.5.1 (public site)~~ | ✅ 2026-02 | Replaced CDN with local vendor |
| ~~8.15~~ | ~~HTTP → HTTPS Google Fonts~~ | ✅ 2026-02 | Fixed in `base_admin.html` |
| ~~9.1~~ | ~~Volunteer programme view~~ | ✅ 2026-02 | Logged-in volunteers see internal events inline with public programme |
| ~~9.2↳~~ | ~~Programming pipeline — approval metadata~~ | ✅ 2026-05-09 | `approval_type` (meeting / standing / not recorded), `approved_at_meeting_date`, `meeting_name`, `meeting_minutes_url` on `Event`; migration `diary/0027`; conditional form section in event edit (meeting-specific fields hidden unless "Approved at meeting" selected); server-side validation requires date when type is meeting. |
| ~~9.3↳~~ | ~~Collapse rota notes by default~~ | ✅ 2026-04-28 | Functionality now covered by star/shadow/none toggle on rota entries (9.75). Notes can be shadowed (collapsed) per showing. |
| ~~9.8~~ | ~~Image copyright reminder~~ | ✅ 2026-02 | `IMAGE_COPYRIGHT_GUIDANCE_URL` setting; reminder shown on upload |
| ~~9.9~~ | ~~Break-even calculator for programmers~~ | ✅ 2026-03-02 | Collapsible panel in event edit form beneath terms; pure JS; Finance Collective threshold warnings; fill-level table |
| ~~9.10.2~~ | ~~Clone rota notes with event clone~~ | ✅ 2026-02-28 | `clone_rota_from_showing` now copies `rota_notes`; test added |
| ~~9.10.4a~~ | ~~"Add to calendar" per-showing links (MVP)~~ | ✅ 2026-04-28 | Per-upcoming-showing links: `.ics` download (universal), Google Calendar, Outlook.com — surfaced on both public event page (`view_event.html`) and volunteer rota (`edit_rota.html`). New `toolkit/diary/calendar_links.py` (hand-rolled iCalendar, no new dep); `single-showing-ics` view on `Showing.objects.public()`; `{% calendar_links %}` inclusion tag; `_calendar_links.html` partial. Hidden for past or cancelled showings. 7 new tests. Precursor to subscribable feeds in 9.10.4. |
| ~~9.10.5~~ | ~~Role timing notes field~~ | Shelved | Merged with 9.40 — both add time fields to rota. Deferred: may add visual clutter and more fields for programmers to fill. Revisit if needed. |
| ~~9.10.6~~ | ~~Inline warning when rota notes carry to cloned showing~~ | ✅ 2026-04-30 | `clone_source_showing` added to context in `edit_views.py`; amber alert banner in "Add a showing" form when source showing has rota notes. |
| ~~9.10.7~~ | ~~Clone event as new event~~ | ✅ 2026-03-02 | "Clone as new event" button on Event Hub; copies all text/config fields (copy, terms, notes, pricing, etc.) + tags + rota from source event; new Showing created unconfirmed; ticket link intentionally left blank; 7 new tests; `devserver_settings` now silently skips `debug_toolbar` when not installed (fixes Docker test runner); `dev.txt` updated to `fixtures>=4` |
| ~~**9.12**~~ | ~~**"Dormant" volunteer status**~~ | ✅ 2026-05-05 | `Volunteer.status` CharField (active/dormant/retired); `active` BooleanField kept as derived field synced by `save()`; migration `members/0012`; volunteer edit form gains status radio buttons; volunteer list shows dormant badge + "Show dormant" filter link; `activate_volunteer` and `anonymise_volunteer` views updated; 138 tests pass. |
| ~~9.16~~ | ~~Alt text fields for event images~~ | ✅ 2026-03-02 | `MediaItem.alt_text` field + migration `diary/0012`; `MediaItemForm` gains field with optional guidance URL; `MediaItemAdmin`; `ALT_TEXT_GUIDANCE_URL` setting + context; all 7 image `<img>` tags updated to use `alt_text` with fallback; Hub shows Alt text row above Image credit; seed data sets `alt_text` to "Poster for {event.name}" |
| ~~9.17↳~~ | ~~`Role` badge flags: beginner-friendly, wheelchair-inaccessible, keyholder-only~~ | ✅ 2026-03-03 | `beginner_friendly`, `not_wheelchair_accessible`, `keyholder_only` BooleanFields on `Role`; migrations 0016+0017; editable in roles page (🌱/♿✗/🔑 columns); rota `<li>` emits `data-*` attributes; JS ROLE_BADGES switches from name-matching to attribute-driven; seed data sets flags on correct roles |
| ~~9.18.1~~ | ~~Supercharge EventTemplate~~ | ✅ 2026-03-02 | `EventTemplate` gains `copy`, `copy_summary`, `terms`, `film_information`, `private`, `outside_hire`, `rota_notes`. `EventTemplateRole` through model adds per-role `count` field — e.g. "5 × Cleaner". Applied to new events in `Event.__init__` and `reset_rota_to_default()`. Template edit UI replaced: list page + per-template detail/edit page with inline role formset + JS "add row". Migrations `diary/0013` + `0014`. Seed data updated. |
| ~~9.18.3~~ | ~~Fix action button order: Edit → Clone → Delete~~ | ✅ 2026-02-28 | Delete moved to bottom of `form_showing.html`; Clone/add-date link added above it |
| ~~9.20.1~~ | ~~Test: datetime-local POST format end-to-end~~ | ✅ 2026-03-07 | T-format POST test in `EditShowing`; 3 widget unit tests for `JQueryDateTimePicker.value_from_datadict` |
| ~~9.20.2~~ | ~~Test: `ROTA_CLEAR_EMAIL_PROMPT_ENABLED` in context~~ | ✅ 2026-03-07 | Two `@override_settings` tests in `EditRotaViewGet` |
| ~~9.20.3~~ | ~~Test: volunteer programme view (logged-in vs anonymous)~~ | ✅ 2026-03-07 | `VolunteerProgrammeView` class in `test_public_views.py`; 3 tests covering anon vs authenticated and `is_volunteer` context flag |
| ~~9.20.4~~ | ~~Test: `IndexLink.description` field save/render~~ | ✅ 2026-03-07 | `test_create_link_with_description`, `test_edit_link_with_description`, `test_description_rendered_on_index` |
| ~~9.20.5~~ | ~~Test: word counter script present in edit-event GET~~ | ✅ 2026-03-07 | `assertContains(response, "word-counter")` in `test_get_edit_event_form_no_media_no_legacy_copy` |
| ~~9.21~~ | ~~Recurring events / clone-to-dates~~ | ✅ 2026-05-25 | "Add showings on multiple dates" page on each event. Flatpickr multi-date picker (up to 52 dates); shared start time, room, booked_by; all showings created unconfirmed; rota cloned from latest showing. `MultiDatePickerWidget` + `BatchAddShowingsForm` + `batch_add_showings` view + `batch-add-showings` URL. "Add showings on multiple dates →" button on event hub. 11 new tests. |
| ~~9.22 / 9.30 (hire_name)~~ | ~~Hirer name on outside hire events~~ | ✅ 2026-05-13 | `Event.hire_name` CharField + migration `diary/0031`. Shown in event hub beneath Outside hire row (when set), and appended to the amber Outside hire badge on both `edit_rota.html` and `view_rota.html`. Added to `EditEventForm` fields and `form_event.html` tooltip map. |
| ~~9.23~~ | ~~"Films start on time" banner~~ | ✅ 2026-04-16 (updated 2026-04-28) | `FILMS_START_ON_TIME` setting (`False` in common, `True` in `settings_ss.py`); `films_start_on_time` context var in `view_event`; conditional `<p class="films-start-on-time">` in S+S template; subtle italic styling in `event_custom.css`. **Updated:** Now only shows for events with the "film" tag — `public_views.py` checks `event.tags.filter(name="film").exists()` |
| ~~9.24~~ | ~~Pronouns on hover for rota names~~ | ✅ 2026-05-09 | Reused existing `Member.personal_pronouns` (no new model field). `EditRotaView` builds a name→pronouns lookup from active volunteers and attaches `pronouns` to each prefetched rota entry; `edit_rota.html` renders `title="…"` on the rota name span when set. Public/printable `view_rota.html` deliberately left unchanged (PII per spec note). Seed data: 10 of 20 demo volunteers given pronouns; toml gains optional `pronouns =` per entry. Test: `test_rota_name_pronouns_tooltip`. |
| ~~9.25~~ | ~~Tap to sign up on rota (mobile)~~ | ✅ 2026-05-12 | Implemented as part of 8.1 MVP: `configureVolunteerTapToToggle()` in `edit_rota.js` handles tap-to-claim and tap-to-clear for all linked volunteers; superusers retain jeditable free-text. |
| ~~9.26~~ | ~~Event resource links (generalised rota links)~~ | ✅ 2026-03-02 | `EventLink` model + migration `diary/0015`; `validate_event_link_url` domain whitelist (`riseup.net`, `nextcloud.com/org`, `chat.whatsapp.com`, `linktr.ee`; `EVENTLINK_EXTRA_ALLOWED_DOMAINS` setting); inline formset with progressive reveal (1 row visible, "+ add" up to 3); chip display on rota + event hub; admin inline; seed data (50/30/15/5% link distribution); `fixtures>=4` added to `requirements/docker.txt` |
| ~~9.26.1~~ | ~~EventLink templates — pre-populate links from event template~~ | ✅ 2026-05-07 | `EventTemplateLink` model + migration `diary/0025`; `EventTemplateLinkFormSet` in forms.py; progressive-reveal link formset on template edit page; links copied to `EventLink` records in `add_event` view when event created from template; same domain whitelist enforced. |
| ~~9.28~~ | ~~Volunteer role tier labelling + GDPR danger indicators~~ | ✅ 2026-03-02 | "Panopticon access" label; Programmer BooleanField; red `danger-zone` card with ⓘ GDPR tooltips on Programmer + Panopticon fields |
| ~~9.29~~ | ~~Role management — one-shot roles per showing~~ | ✅ 2026-06-14 | `Role.is_one_shot` BooleanField + migration `diary/0085`. One-shot roles are created on the fly in the showing edit form and never appear in the permanent roles list. Multiple one-shots per showing. Cleanup: after any `update_rota()` call, orphaned one-shot roles (no RotaEntries) are deleted. One-shot slots appear on the rota with a ✦ marker. Cloned showings carry their one-shots; reset-to-default clears them (correct). 915 tests green. |
| ~~9.30~~ | ~~Outside hire enhancements — external crew notes field~~ | Shelved 2026-05-25 | `hire_name` is done. `external_crew_notes` dropped — rota notes are sufficient for this edge case. |
| ~~9.31~~ | ~~Beginner-friendly rota slot highlighting~~ | ✅ 2026-04-28 | Filter added to rota filterline; beginner-friendly roles (🌱 badge) can be filtered; auto-highlighted in rota display |
| ~~9.32~~ | ~~Rota past-date navigation~~ | ✅ 2026-05-12 | Non-superusers can now navigate back up to 31 days. Datepicker `min` updated in JS; server-side `EditRotaView` clamps to same floor. Past entries display read-only; POST handler already rejects edits to in-past showings. |
| ~~9.33~~ | ~~Calendar key overhaul~~ | ✅ 2026-03-01 | `Room.is_primary` field + migration; vivid red/blue/yellow primary rooms, pastel secondary rooms; `_is_light_colour()` auto-applies black `textColor` (Café yellow); CSS filter approach dropped — colours stored directly; historic colour removed, red nowIndicator line replaces it; collapsible sticky sidebar key on both views; list view restructured to per-month `<table>` blocks (`<h2>` outside table fixes colspan vertical-line breaks), IDEAS removed, room header gets full-width colour stripe via `border-bottom`, empty-day rows use full column structure; 380 tests pass |
| ~~9.33 (seed)~~ | ~~S&S spaces: seed data~~ | ✅ Already done | `rooms.toml` has 9 rooms (Cinema, Venue Space, Café, External, Meeting, Dark Room, Print Room, Workshop, Green Room). |
| ~~9.33 (view)~~ | ~~Diary column-per-room view~~ | ~~Closed 2026-05-25~~ | Layout already present in the diary list view. No further work needed. |
| ~~9.34~~ | ~~"Showing" terminology review~~ | ~~Decided 2026-05-25~~ | Keep "Showing" — people are used to it; it's the best of a bad bunch. No code change. |
| ~~9.35~~ | ~~1-click top nav access to Diary + Rota edit~~ | ✅ 2026-03-02 | Diary + Rota + Calendar promoted to top-level nav items; nav order Diary → Calendar → Rota → Website → Meta-programming → Members → Volunteers; index page redesigned as two-column Bootstrap card grid with tier badges (All users / Programmer+ / Panopticon only); "Meta" renamed "Meta-programming"; custom links kept as bottom section; logout removed from body |
| ~~9.35.1~~ | ~~Toolkit homepage: dashboard section above link directory~~ | ✅ 2026-05-16 (updated 2026-05-25) | Dashboard section above existing link directory. Widgets: upcoming shifts, starred events, new since last login, rota gaps, unconfirmed showings, inductions & training, bulletins, shopping needs (added once 9.88 landed). All computed in `ToolkitIndexView.get_context_data()`; no new views. 10 new tests. |
| ~~9.36~~ | ~~Vacancies page as email generation tool~~ | ✅ 2026-05-13 | Vacancies page rebuilt with Bootstrap cards, urgency colour-coding (red=today, orange=tomorrow), role filter pills (client-side), "Draft email" button that generates plain-text vacancy list and copies to clipboard. View updated to pass `all_role_names` for filter. |
| ~~9.37~~ | ~~Public programme keyword search~~ | ✅ 2026-05-23 | Keyword search input merged into the volunteer-notice row (notice left, search right; left-aligned when no notice). Client-side: filters `.showing` cards by `data-search-text` (name, pre/post-title, copy, tags), hides matches with `filter-hidden` class, reflows masonry. URL persistence (`?search=`). "Clear" button. Switches list→grid when active. New `programme.js` + CSS in `programme.css`. Tag filtering left to nav Programme dropdown. S+S template only. |
| ~~9.38~~ | ~~Toolkit page: last login display + diary/edit pre/post title hide~~ | ✅ 2026-03-07 | Both already implemented: login/tier status line in `toolkit_index.html` lines 204–218; edit list already shows only `event.name` (no pre/post titles) |
| ~~9.39~~ | ~~Quick create event for keyholders~~ | ✅ 2026-05-13 | `/diary/edit/event/open` — three-field form (date defaults to today, opens_at 14:00, closes_at 18:00, optional note). Creates private confirmed Event from "Building Open" template; closing time stored as `final_volunteer_time` so it appears on the rota. "🔑 Building open" button added to diary edit index. |
| ~~**9.40**~~ | ~~**Setup / doors-open / final-volunteer times on showings**~~ | ✅ 2026-05-05 | Three nullable `TimeField`s on `Showing`: `setup_time`, `doors_time`, `final_volunteer_time`; migration `diary/0023`; surfaced in `ShowingForm`; displayed in `edit_rota.html` and `view_rota.html` when any are set. |
| ~~9.41~~ | ~~Clickable legend room filter (calendar)~~ | ✅ 2026-05-25 | Room checkboxes already in filter bar from 9.41+9.37. Added: `sessionStorage` persistence for all filter state (saved on every `applyFilters()`, restored on init with `syncFilterUI()`); "All / None" toggle links above room list. |
| ~~9.41 + 9.37~~ | ~~Calendar: filtering + visual polish~~ | ✅ 2026-04-06 | **Filtering:** Time-of-day (All/Daytime/Evening), Name search (text input), Status (Unconfirmed/Private/Outside hire/Cancelled toggles with visual indicators matching event styling), Tags (multi-select button group), Rooms (multi-select checkboxes with primary/secondary visual distinction). **Key integrated into filterline:** Visual legend row shows indicators (dashed box=unconfirmed, italic=private, bold=outside hire, strikethrough=cancelled). **Visual polish:** Compact emoji badges on events (🔒 private, 👥 outside hire, 🚫 cancelled, ✻ discounted). **Mobile UX:** Filter bar auto-collapses on mobile (<768px) with toggle button to expand; maximises calendar real estate on small screens. **Technical:** Extended `edit-diary-data` JSON endpoint with `hour` and `tags`; client-side filtering via `event.setProp('display', 'none')`; 150ms debounce on name search. All 275 tests pass. |
| ~~9.42~~ | ~~Tests: diary edit list view~~ | ✅ 2026-04-30 | 5 tests in `EditDiaryListViewTests`: rooms context has no None; month-heading present; multiroom shows room names; single-room shows `<th>Event</th>`; empty days render rows. |
| ~~9.43~~ | ~~Room management UI~~ | ✅ 2026-05-07 | `edit_rooms` (list + create) and `edit_room_detail` (edit/delete) views; `RoomForm` with native `<input type="color">` picker + live preview; delete blocked if showings reference the room; linked from Meta-programming section of toolkit index (Programmer+ access). |
| ~~9.45~~ | ~~Password management in volunteer profile~~ | ✅ 2026-05-07 | Panopticon-only section added to Permissions card in volunteer edit page: "Send reset email" button (primary flow, calls `send_volunteer_password_reset` view, uses `default_token_generator`) + collapsible "Set password directly" form (uses Django `SetPasswordForm`). Both POST-only, `is_superuser` gated. |
| ~~9.46~~ | ~~Login page styling~~ | ✅ 2026-03-02 | Extends `base_public.html`; login, logout + password reset templates styled with centered card layout; friendly titles; `site_custom.css` moved into `base_public.html` so all descendants get nav styling; see TASKS.md 9.46 |
| 9.47 | Rota role display order | Deprioritised | Leave as-is for now. Very low priority. |
| ~~9.48~~ | ~~Template export/import~~ | ✅ 2026-05-25 | JSON export textarea on each template detail page (superuser only). Import page at `/diary/edit/eventtemplates/import/` (superuser only): paste JSON, checkbox to overwrite-or-copy-on-name-collision. Roles and tags matched by name; unknown items skipped with warning message. `_export_template_json()` helper + `import_event_template` view. "Import from JSON" link on template list (superuser only). 15 new tests; 702 total, all passing. |
| ~~9.49~~ | ~~Runtime-configurable permissions via SiteSettings~~ | ✅ 2026-06-15 | 11 `perm_*` fields on `SiteConfiguration` (migration 0086). `feature_required(feature_name)` decorator reads level at request time. `feature_perms` context dict in context processor drives nav hiding. All 11 configurable views re-decorated. Perm table in Site Settings now renders editable dropdowns for configurable rows; fixed rows show lock badge. Nav guards updated in `base_admin.html`, `fragment_navmenu.html` (desktop + mobile). 916 tests passing. **Extension (2026-06-15):** Diary read/write split: `perm_diary_edit` renamed `perm_diary_read` (default Volunteer); `perm_programming_queue` split into `perm_programming_queue_read` (Volunteer) + `perm_programming_queue_write` (Programmer+); migration 0087. Diary list template: edit controls (+ slots, date links, New Event, Building Open) conditionally hidden for read-only users via `can_edit` context; event name/hub links stay visible. Programming queue template: "New event" button hidden for read-only users. `panopticon_required` decorator refactored: now raises 403 for authenticated non-superusers (was 302) and sets `request.view_access_level`. `feature_required` also sets `request.view_access_level`. Quiet access-level footer badge on all admin pages showing resolved label (default "All volunteers"). Security/permission tests updated to reflect correct 302/403 semantics. 916 tests green. |
| ~~9.50~~ | ~~Volunteer self-service profile edit from nav~~ | ✅ 2026-03-02 | Own name in top nav links to `edit-volunteer/<pk>`; guarded by `user.volunteer.pk`; `seed_dev_data` now creates Member+Volunteer for all demo accounts so link appears in dev |
| 9.51 | Working groups subscribe/unsubscribe page | 🔴 XL — backlog | Page exists at `/toolkit/working-groups/`. No simplelists integration yet — deferred to 9.87. Dev/sysadmin conversation needed before scoping further. See TASKS.md 9.51. |
| ~~9.53~~ | ~~Show end time on the rota~~ | ✅ 2026-03-07 | `–HH:MM` appended to start time in `edit_rota.html` and `view_rota.html`; guard on `event.duration`; no model change |
| ~~9.54~~ | ~~Structured event cost terms~~ | ✅ 2026-06-11 | `cost_type` dropdown (film_license/performer_fee/venue_hire/internal/tbc) + conditional sub-fields (`cost_distributor`, `cost_flat_fee_gbp`, `cost_fee_includes_vat`, `cost_percentage_split`, `cost_minimum_guarantee_gbp`, `cost_total_gbp`) on `Event` and `EventTemplate`. `technical_notes` field for AV/tech rider notes, separate from financial terms. JS show/hide: sub-fields appear/disappear based on cost_type selection. Terms word-count validation waived when cost_type is set (and not TBC). Gated behind `SiteConfiguration.structured_cost_terms_enabled`. Event hub shows structured cost summary + technical notes when flag on. All cost fields pre-populate from template on event creation. Migration `diary/0077`. 895 tests green. |
| ~~9.57~~ | ~~Placeholder image generator for new events~~ | ✅ 2026-04-28 |
| ~~9.58~~ | ~~Programme: one card per showing~~ | ✅ 2026-05-25 | S+S programme grid (`view_showing_index.html`) now renders one card per showing instead of one card per event with stacked dates. The showing's date is always bolded (it IS the date for this card). `_view_diary` builds both `showings_grid` (flat list for S+S template) and `events` (OrderedDict for Cube template) in a single pass. `_EventShowings` wrapper class removed. |
| ~~9.59~~ | ~~Programmer-defined focal point for index images~~ | ~~Obviated 2026-06-11~~ | Covered by 9.107 letterbox bars + crop selector (Option B unified UI). No separate focal-point feature needed. |
| ~~9.60~~ | ~~Room name and colour on the rota~~ | ✅ 2026-03-27 | Added room name with coloured left-border accent to event heading row in `view_rota.html`; behind `MULTIROOM_ENABLED`; no model/view changes; will need updating once 9.7 adds multi-room bookings |
| ~~9.61~~ | ~~Quick links from event detail to rota and event hub~~ | ✅ 2026-04-25 (extended 2026-05-13) | Volunteer-only "View rota" + "Event hub" links on `view_event.html`. Extended: links now deep-link to the specific showing's month view (`rota-edit-month` URL); `id="showing-{pk}"` anchor on each `.rota-showing` div; amber left-bar highlight + bg fade on arrival; `scroll-margin-top` on `.rota-showing` so sticky day header does not mask the linked entry. Same links added to event hub showing cards. |
| ~~9.62~~ | ~~Multi-room bookings architecture~~ | ✅ 2026-05-15 (extended 2026-05-16) | `RoomBooking` model (Showing → Room with start/end/notes); `Room.map_slug` field (links diary rooms to SVG floorplan elements); `Showing.room` FK removed; `rooms_display` + `primary_room` helpers on `Showing`. Migration 0032 backfills existing room assignments. Showing edit page: simple room dropdown (single-room path) + collapsible advanced panel with SVG building map (click to select rooms) + inline formset for start/end/notes per room. Non-blocking clash detection: amber alert when a confirmed booking overlaps in the same room. `clash.py` module. **Extended:** `Room.show_column` BooleanField (migration 0035); diary list groups rows by room booking start time (not showing start); `showing_for_room_at` simple_tag replaces old filter; `other_room_bookings_at` tag finds non-column bookings; narrow "Other" column shows coloured pills for minor-room bookings; 4 minor rooms activated in seed data (Kitchen, Snug, Projection Booth, Middle Corridor) with `show_column=false`; room edit UI exposes show_column. 515 tests pass. |
| 9.63 | Room availability overlay on SVG building map | 🟡 M — not priority | Map integration in showing edit is done. Remaining: show existing bookings on the map so programmers see clashes visually. Helpful but not a priority. |
| ~~9.64~~ | ~~Calendar: FC6 migration + month view~~ | ✅ 2026-04-05 | Migrated from FC3.5.1 + Scheduler 1.7.1 to FC6.1.20 global bundle. Full JS rewrite (`calendar_index.js`): jQuery-free FC init, FC6 API (`headerToolbar`, `initialView`, `datesSet`, `eventDidMount`, `dateClick`, `dayMaxEvents`). Removed `htmlPrefilter` workaround. CSS updated (`.fc-title` → `.fc-event-title`, `.fc-day.fc-today` → `.fc-day-today`). Calendar now full-viewport-width (Bootstrap container override). Dev setup switched to `runserver` + bind-mount (no collectstatic for JS/CSS changes). Month view confirmed working. |
| ~~9.65~~ | ~~Panopticon site settings dashboard~~ | 🟡 M | Edit runtime settings (MAX_COUNT_PER_ROLE, etc.) without redeploy. Singleton SiteConfiguration model + Panopticon CRUD view. **Status:** Needs spec by Jonny+Claude |
| ~~9.66~~ | ~~Film event metadata + OMDb integration~~ | ✅ 2026-06-11 (migrated to OMDb 2026-06-22) | `Film` model (title, year, director, runtime, countries, languages, certificate, IMDB ID, poster path, overview). `media_type` field covers TV shows. FK from `Event.film` (`SET_NULL`, nullable). Initially used TMDB; migrated to OMDb (`omdb.py`, stdlib only, migration `diary/0083`): `/search/movie` + `/t/{imdb_id}`. API key in Site Settings → External APIs. Three-state film section in event edit form: linked summary card (source badge "via OMDb" / "manually entered"; OMDb tooltip; "Edit details" button for manual entries; manual form pre-filled on link or page-load so Unlink + "Enter manually instead" lets programmer correct one field) / OMDb search box / manual entry form. Film section auto-expands when event has "film" tag. Completeness bar badge when event has "film" tag but no Film linked. Certificate lookup URL configurable. 34 tests. 5 seed Film records. Poster import deferred to 9.141. |
| ~~9.67~~ | ~~Room scratchpad/notes~~ | ✅ Obviated by 9.77 — Building Map's `RoomNote` model covers this (click room → side panel → add/edit note). |
| ~~9.68~~ | ~~Collectives directory + membership~~ | ✅ Covered by 9.68.1 (public directory with `public_copy` + `listed_publicly`) + 9.86 (`Volunteer.collectives` M2M + opt-in directory). |
| ~~9.68.1~~ | ~~Collectives public directory: `public_copy` + `/collectives/` page~~ | ✅ 2026-05-23 | `public_copy` TextField + `listed_publicly` BooleanField on `Collective`; migration `labs/0013`. New public view at `/collectives/` (no login). Crispy form Layout adds a "Public directory" section to the collective edit form. `star_and_shadow_templates/collectives_public.html` extends `base_public.html`. Route registered in both `urls_flat.py` and `urls.py`. |
| ~~9.68.2~~ | ~~Collective sample role field~~ | ✅ 2026-06-16 | `sample_role` TextField on `Collective` (migration `labs/0023`). Shown in internal card under "Example of what you might do" (after get_involved). Shown on public directory as "Example role:". Exposed in `CollectiveForm` with 3-row textarea. 168 labs tests pass. |
| ~~9.128~~ | ~~Collective defined roles (CollectiveRole model)~~ | ✅ 2026-06-16 | `CollectiveRole` model (title, description, time_commitment, getting_started, needs_volunteers, open_to_new_volunteers, display_order) with FK to `Collective` (related name `defined_roles`). Inline formset on collective edit page (`CollectiveRoleForm` + `CollectiveRoleFormSet`; custom `__init__` sets boolean initials to False for extra rows so empty rows don't trigger validation). Collapsible "Defined roles" section in internal collectives card with recruiting badge. Public directory shows open roles with "looking for people" badge. `CollectiveRoleInline` on `CollectiveAdmin`. Migration `labs/0024`. 170 labs tests green; 916 total. |
| ~~9.69~~ | ~~Event detail showing date UX improvements~~ | ✅ 2026-05-23 | Past showings hidden; next future showing bolded (`showing-date--next`); future showings beyond `max_showing_dates_shown` (default 5, site-configurable) collapsed into a `<details>` disclosure. Finished events show "This event has finished." Volunteer rota link deep-links to the next showing's month (fallback to first if all past). Migration `diary/0044`. |
| ~~9.70~~ | ~~Nightly production DB backup~~ | Shelved | Production team has their own backup process. |
| ~~**9.71**~~ | ~~**Event terms and financial field change log**~~ | ✅ 2026-04-22 | `EventTermsRevision` model + migration `diary/0020`; `pre_save` signal in `diary/signals.py`; `DiaryConfig.ready()` in `diary/apps.py`; `_saved_by` wired in `EditEventView.post()`; collapsible change history panel in `view_event_privatedetails.html`; `EventTermsRevisionInline` in `EventAdmin`; 13 new tests in `test_terms_revision.py` |
| ~~**9.71**~~ | ~~**Event terms and financial field change log**~~ | ✅ 2026-04-22 | See above. |
| ~~**9.72 MVP**~~ | ~~**Role deletion cascades silently to rota entries (MVP fix)**~~ | ✅ 2026-05-05 | `RotaEntry.role` changed from `CASCADE` to `PROTECT` (`diary/models.py:791`); migration `diary/0022`. Django now raises `ProtectedError` rather than silently deleting historical rota entries when a role is deleted. Soft-delete (`active` flag + UI) deferred to phase 2. |
| ~~**9.72 full**~~ | ~~**Archival pattern for roles and tags; volunteer summary status**~~ | ✅ 2026-05-06 | `Role.archived` and `EventTag.archived` BooleanFields added; migration `diary/0024`. `delete()` overridden on both models: archives instead of deleting if the item has been used in rota entries / events. Edit UIs updated: delete column removed, replaced with Archive button per row; archived items shown at the bottom with Restore. Volunteer summary now includes dormant volunteers with a Status column; count line shows active + dormant breakdown. Showing edit form: `<table>` wrapper removed from `{% crispy form %}` output — the three time fields (`setup_time`, `doors_time`, `final_volunteer_time`) now render correctly. |
| ~~9.73~~ | ~~Outside hire flag prominent on rota~~ | ✅ 2026-04-16 | Amber `Outside hire` badge in showing header on `edit_rota.html` (`.outside-hire-badge` CSS class) and `view_rota.html` (inline style in `event_head` row). No model change. |
| ~~9.74~~ | ~~Calendar: timeline week + month views~~ | ✅ 2026-04-05 | Added `resourceTimelineWeek` and `resourceTimelineMonth` views for multi-room venues. FC6 Scheduler bundles (`resource.global.min.js`, `resource-timeline.global.min.js`) vendored. Views show rooms as rows. Added `schedulerLicenseKey` for GPL exemption. Events open in new tab (`window.open`). `localStorage` remembers last view. Mobile CSS for toolbar stacking and touch targets. Fixed test regex for 5-arg `init_calendar_view` (was 6 args with jQuery in FC3). |
| ~~**9.74**~~ | ~~**Permission model redesign**~~ | ✅ 2026-05-25 | `panopticon_required` decorator added to `toolkit_auth/decorators.py`. All `member_views.py` (read + write) and all volunteer PII views changed to `panopticon_required`. `edit_volunteer`: editing others now `is_superuser` only; volunteers still edit own profile. `activate_volunteer` redirects to summary. Template fix: GDPR section guarded by `volunteer.id`. Test mixin: `admin` promoted to superuser. 12 new permission-boundary tests. 676 total, all passing. |
| ~~9.75~~ | ~~Starred + shadowed events on the rota~~ | 🔵 S | ✅ 2026-04-25 — VolunteerEventMark (one mark per event); single ☆→★→☽ toggle cycling; ☽ collapses showing to title; filterline: starred filter + beginner highlight + vacancies filter. |
| ~~9.76~~ | ~~Rota date navigation and orientation~~ | ✅ Done | ~~9.76.1~~ ✅ 2026-04-30; ~~9.76.2~~ ✅ 2026-04-30; ~~9.76.3~~ not doing; ~~9.76.4~~ closed 2026-05-25 (month nav works well; no rail needed); ~~9.76.5~~ not doing. |
| ~~9.77~~ | ~~Labs: Building Map~~ | ✅ 2026-04-28 | New `toolkit/labs` Django app. `/labs/floorplan/` renders an interactive SVG floorplan of the Star and Shadow building (33 rooms). Any logged-in user can click a room to open a side panel and add/edit a note. Notes stored in `RoomNote` model (per-room, one per room). Amber highlight on rooms with notes; blue highlight on selected room. `Labs` nav entry in both Bootstrap admin nav and public sidebar nav. `@login_required` on both views. | "Generate" button next to file upload creates typographic poster via AJAX. Colour picker lets users choose accent colour (defaults to event tag colour). Preview shown immediately with option to remove. Form save attaches pre-generated MediaItem. Extracted `_make_poster_image` to `diary/poster.py`; AJAX endpoint accepts `colour` param; `generated_media_id` hidden field wires it to form save. |
| ~~9.78~~ | ~~Donation specifier~~ | ✅ 2026-05-06 | Public `/labs/donations/` page (no login) + internal `/labs/donations/manage/` (login required). Traffic-light status badges; items grouped by category. Manage page: expandable row drill-down — compact summary (name, category, status badge, last-edited-by), click to open full edit panel (public notes, internal notes, contact, order, active). `DonationItem` gains `internal_notes`, `last_edited_by`, `updated_at`. Seed data: 13 sample items across 4 categories. |
| ~~9.79~~  | ~~Tool library / community exchange~~ | ✅ 2026-06-01 | `ExchangeItem` model (listing_type: lend/give; category; condition; owner_type/owner_volunteer; status; image). Migration `labs/0019`. `community_exchange_enabled` SiteConfiguration flag + migration `diary/0055`. Views: list (filterable by type + category), detail, add, edit, withdraw. Templates: card grid, detail, form. Nav links (base_admin + fragment_navmenu) gated on flag. Django admin registered. 27 new tests; 808 total, all passing. |
| ~~9.80~~ | ~~Non-rota jobs / maintenance schedule~~ | ✅ 2026-05-06 | `/labs/jobs/` table-format jobs board matching the existing spreadsheet. Fields: date, area, description, safety risk ⚠️, skill needed 🔧, keyholder required 🔑, location type (building/remote/both) 🏠/🏡/🔀, reporter name, plan/status, claimed by. Urgency sorted semantically (high→medium→low). `Job` model overhauled: `location`+`skills` removed; `area`, `safety_risk`, `skill_needed`, `location_type`, `reporter_name`, `plan_status`, `resolved`/`resolved_at` added. Seed data: 6 jobs including one resolved example. |
| ~~9.81~~ | ~~Building maintenance & annual jobs checklist~~ | ~~Shelved 2026-05-25~~ | Existing spreadsheet works well. Likely feature overreach; let the collective self-organise. |
| ~~9.82~~ | ~~Rota badge visibility toggles~~ | ✅ 2026-05-07 | Three localStorage-persisted checkboxes in the rota filterline: 🌱 Beginner-friendly, ♿ Wheelchair access, 🔑 Keyholder only. Toggle visibility of corresponding badge spans via CSS wrapper classes (`rota-hide-beginner` / `rota-hide-wheelchair` / `rota-hide-keyholder`). Keyholder badge class updated from bare `role-badge` to `role-badge--keyholder` to enable targeting. No model changes. |
| ~~9.83~~ | ~~Calendar: hide early-morning hours + filterline slot trimming~~ | ✅ 2026-05-12 | 3-day and week views now default to `slotMinTime=10:00` (configurable via `SiteConfiguration.calendar_slot_min_hour`). Daytime/evening filterline buttons now also trim the visible hour range in time-grid views, not just the resource timeline. `calendarConfig` argument added to `init_calendar_view`. Migration `diary/0029`. |
| ~~9.84~~ | ~~Calendar: shorten toolbar button labels on mobile~~ | ✅ 2026-05-12 | `isMobile` flag used at calendar init to choose short labels (Mo/Wk/3d/Wk·R/Mo·R) on ≤768px screens; full labels on desktop. |
| ~~9.85~~ | ~~Rota: ⚠ warning badge for showings with no keyholder assigned~~ | ✅ 2026-05-12 | `applyKeyholderWarnings()` in `edit_rota.html` scans future showings; appends `⚠ keyholder` badge next to event name when any `data-keyholder="1"` slot is empty. Re-runs on `rota:entry-changed` so badge disappears as soon as the slot is filled. |
| ~~9.86~~ | ~~Volunteer opt-in directory~~ | ✅ 2026-05-21 | `dir_share_name` (full/initial/none), `dir_share_pronouns/email/phone/access_rider/collectives` BooleanFields on `Volunteer`; `access_intro/needs/links` TextFields (access rider); `emergency_contact_name/relationship/phone` (panopticon-only); `collectives` M2M to `labs.Collective`; `directory_display_name()` helper. Migration `members/0014`. `/toolkit/volunteers/directory/` (login-required). `VolunteerForm` updated with new sections. `form_volunteer.html`: Access rider card, Collectives + directory card, Emergency contact card (panopticon-only). `volunteer_directory.html`: card grid, name search, pronouns/email/phone/collectives/access-rider per card. Anonymise view clears new PII fields + `collectives.clear()`. 15 new tests. |
| 9.87 | Collectives → simplelists email list sync | 🟡 M — backlog | High importance but complex. Defer until prod access available. Key risk: sync state for users who unsubscribe outside our interface. Uses simplelists form POST (no API). See TASKS.md 9.87. |
| ~~9.88~~ | ~~Shared shopping list (consumables)~~ | ✅ 2026-05-25 | `ConsumableItem`, `SupplierRecord`, `NeedFlag`, `ProcurementPledge` models (migration `labs/0015`). Any volunteer can add items, flag as needed, pledge to get it, mark resolved. Supplier info (Panopticon admin only) with account-holder pointer. Dashboard widget on toolkit index. Shopping section in weekly digest email. Nav link in Labs dropdown. Access Levels moved into Labs dropdown. 32 new tests; 755 total, all passing. |
| ~~**9.89**~~ | ~~**Weekly volunteer digest email**~~ | ✅ 2026-05-25 | `Volunteer.weekly_digest` + `last_digest_sent_at` fields (migration `members/0017`). Opt-in toggle on `form_volunteer.html`. `send_volunteer_digest` management command: personalised plain-text email (upcoming shifts, new programme, starred events, shopping needs). HMAC unsubscribe link (`/volunteers/digest/unsubscribe/`). systemd timer docs in `ONBOARDING.md`. Shopping section added once 9.88 landed. |
| ~~9.90~~ | ~~Access transparency: privileged user list + rights explanation~~ | ✅ 2026-05-25 | `/toolkit/access/` — plain-language rights table (Volunteer/Programmer/Panopticon tiers); list of current Panopticon users with grant reason, date, last reviewed; amber highlight for overdue reviews; "Mark reviewed" button (Panopticon only). Programmer list with grant date. `PanopticonGrant` + `ProgrammerGrant` models (migration `members/0016`); `UserForm` creates/deletes grants on save; `panopticon_reason` field with JS show/hide. `toolkit_access` + `mark_panopticon_reviewed` views. "Access levels" nav link for all authenticated users. 16 new tests. |
| ~~9.91~~ | ~~Dashboard widget: upcoming showings with rota gaps~~ | ✅ 2026-05-18 | All-users card: confirmed showings in the next 21 days with 3+ unfilled required slots. Thresholds (count and/or percentage) configurable via SiteConfiguration. `rota_gap_min_missing` (default 3) and `rota_gap_min_pct` (default 0) fields added to model + migration 0036 + site settings form. Badge shows missing count. Widget suppressed when both thresholds are 0. |
| ~~9.92~~ | ~~Dashboard widget: unconfirmed upcoming showings~~ | ✅ 2026-05-18 | Programmer+ card: showings in the next 6 weeks still at confirmed=False, ordered by date. `booked_by` shown as muted context. Only appears when data exists. |
| ~~9.93~~ | ~~Dashboard widget: upcoming inductions and training~~ | ✅ 2026-05-18 | All-users card: confirmed showings tagged `induction` or `training-for-volunteers` in the next 6 weeks. `.distinct()` prevents duplicates if both tags present. Only appears when data exists. |
| ~~9.94~~ | ~~Dashboard widget toggles (localStorage)~~ | ✅ 2026-05-18 | Per-browser show/hide for all six dashboard cards. "Customise" button reveals a collapsible panel with checkboxes. IIFE on page load reads localStorage and hides cards before paint. JS sync block wired at bottom of dashboard section. No model change. |
| ~~9.95~~ | ~~Rota: per-volunteer shift highlights + "My shifts" filter~~ | ✅ 2026-05-20 | `rota-showing--mine` (green) and `rota-showing--starred` (amber) CSS highlights; "mine" takes precedence. "My shifts" filter checkbox shown when `current_volunteer_pk` is set. `applyShowingHighlights()` called on init, after mark changes, and after sign-up AJAX. Empty-slot highlight deepened to `#fde68a`. |
| ~~9.95(b)~~ | ~~Bulletins: operational notice board with dashboard banner~~ | ✅ 2026-05-22 | `Bulletin` + `BulletinRead` models in `labs` (migration `labs/0012`); `bulletin_default_expiry_days` (default 30) + `bulletin_guidance` on `SiteConfiguration` (migration `diary/0038`). Views: `/labs/bulletins/`, `/archive/`, `/add/`, `/<id>/{read,pin,expire,delete}/`. Any logged-in user can post; Programmer+ can pin/set expiry; Panopticon only can delete. Dashboard banner on `/toolkit/` shows most-recent unread bulletin with "Got it" POST + link to board if more unread. Active set = explicit `expires_at > now` OR (no expiry AND `created_at` within default window). Pinned-first ordering. Sample seed bulletin added. Nav links added to base_admin and fragment_navmenu. **Note:** TASKS.md numbers this 9.95, but `~~9.95~~` was already used for rota shift highlights; renumber if needed. |
| ~~**9.96**~~ | ~~**Labs app tests — zero coverage (31 endpoints)**~~ | ✅ 2026-05-25 | `toolkit/labs/tests/` created: `common.py` (LabsTestsMixin), `test_security.py` (auth/perm for all endpoints), `test_collectives.py`, `test_bulletins.py`, `test_jobs.py`, `test_donations.py`. Also fixed naive datetime in `job_resolve` view. 108 new tests; 664 total, all passing. |
| ~~**9.97**~~ | ~~**Members model tests — thicken coverage**~~ | ✅ 2026-05-25 | Added `TestMemberModel` (mailout_key auto-gen, uniqueness, no-overwrite on resave; `mailout_recipients()` boundary cases) and `TestVolunteerStatusModel` (status/active sync) to `toolkit/members/tests/test_models.py`. |
| ~~**9.98**~~ | ~~**Split `test_edit_views.py` (2,807 lines)**~~ | ✅ 2026-05-25 | Split into `test_edit_views.py` (342 lines), `test_edit_event.py` (1,290 lines), `test_edit_showing.py` (682 lines), `test_edit_misc.py` (661 lines). All 664 tests pass. No production code changes. |
| ~~9.100~~ | ~~Role qualification gates (MVP)~~ | ✅ 2026-05-31 | Panopticons define named **Qualifications** (e.g. "Bar") at Meta › Roles, then grant them per-volunteer on the profile page. Each role can require a qualification with a **gate**: Off / Advisory (warns but allows sign-up via a browser alert) / Blocking (refuses with HTTP 403). Panopticons always bypass. Gate reads from `VolunteerQualification`, deliberately separate from `TrainingRecord`. **Design decision (2026-05-31):** kept both models rather than consolidating — `TrainingRecord` is load-bearing for General Safety Training (the "Safety trained" date), which has no overlap with role clearances. Only the role-specific TrainingRecord subset overlapped; we simply stop creating those. Verb softened "award"→"grant/record"; fields `granted_on`/`granted_by`. No live-data conversion needed. Migrations: members 0022/0023, diary 0052. |
| ~~9.102~~ | ~~Replace Masonry.js with CSS Grid on programme index~~ | ✅ 2026-06-03 | Masonry.js + imagesloaded removed from `base_public.html`; all Masonry init/layout code stripped from `site-common.js` and `programme.js`; `programme.css` rewritten to use `display:grid` + `grid-template-columns` at each breakpoint (1→2→3→4 cols); images use `width:100%` + `aspect-ratio:2/3` + `object-fit:cover` for uniform portrait cards; nth-child "featured wide card" rules removed. |
| ~~9.108~~ | ~~TicketSource setup guide in event creation flow~~ | ✅ 2026-06-10 | Collapsible "How to set up TicketSource" checklist injected after `ticket_link` field in event edit form. 5-step numbered guide (pricing tier, seating plan, title conventions, URL copy). `film_programming_guide_url` key added to VENUE dict (empty by default; set in venue settings for a link to the full guide). |
| ~~9.109~~ | ~~"Mark as confirmed" as satisfying end-of-creation action~~ | ✅ 2026-06-10 | Green "Everything looks good — ready to go live?" banner replaces the completeness bar when all checks pass but showings are still unconfirmed. For single-occurrence events, includes an inline Confirm button. For series, points to the existing "Confirm all" button. |
| ~~9.110~~ | ~~Configurable age-rating scheme~~ | ✅ 2026-06-10 | `SiteConfiguration.age_rating_choices` JSONField (BBFC defaults: U/PG/12A/12/15/18 seeded in migration 0070). `Event.age_restriction` field: removed hardcoded `choices=`, max_length 16→32. `Event.get_age_restriction_display()` override reads from site config; falls back to legacy labels (`all_ages`/`16_plus`/`18_plus`) for old stored values. `EventForm` builds Select widget choices from site config at form instantiation. "Age ratings" section added to Panopticon site settings page. 6 new tests. |
| ~~9.111~~ | ~~Default rooms on event templates~~ | ✅ 2026-06-05 | `EventTemplateRoom` through-model with `start_delta_minutes` / `end_delta_minutes` / `date_offset`; auto-creates room bookings on showing creation (skips manually-selected rooms); admin inline on `EventTemplateAdmin`. |
| ~~9.112~~ | ~~Multi-day room bookings (date_offset)~~ | ✅ 2026-06-05 | `RoomBooking.date_offset` int field (-1/0/+1); compact "Day" column on Edit Showing room booking table; applies offset in form save. Covers load-in/teardown use cases without dummy Showings. Migration `0063`. |
| ~~9.113~~ | ~~Programming queue for Monday meetings~~ | ✅ 2026-06-05 | `Event.programming_status` (draft/proposed/active/rejected) + `Event.programming_notes`; `/diary/edit/programming-queue/` queue view with approve/return/skip quick actions; "Propose for meeting" button on Event Hub; "Queue" nav link; `programming_status`/`programming_notes` fields in Edit Event form. Migration `0065`. |
| ~~9.114~~ | ~~Programmer UX Tier 1: completeness links + multi-date picker~~ | ✅ 2026-06-05 | Completeness checklist badges are now links to edit form anchors. `number_of_bookings` replaced with flatpickr multi-date picker (`dates` + `start_time`) on New Event form — matches batch_add_showings UX. |
| ~~9.115~~ | ~~Film template placeholder text + validation guard~~ | ✅ 2026-06-05 | Film (DCP/MP4) templates pre-fill pricing, film_information, and terms with placeholder text. `EventForm.clean_terms()` and `clean_film_information()` reject values that still contain `[bracket]` placeholders, preventing accidental submission of unfilled defaults. |
| ~~9.116~~ | ~~New Event form: zoned layout + live template preview~~ | ✅ 2026-06-07 | Two-zone crispy `Layout` on `NewEventForm`: "Book it now" (name, template, date(s), start time) with blue accent vs "Details — amend any time from the Event Hub" (duration, booked_by, private, outside_hire, discounted). Selecting a template renders a live preview (rooms with colour pips, rota with counts, tags, pricing, private/outside-hire flags) and auto-ticks the private/outside-hire checkboxes. `_template_data()` returns a dict embedded via `json_script` (XSS-safe; replaces the old double-encoded `\|safe` string). Submit relabelled "Create event". `room` field dropped from `NewEventForm`/`add_event` (rooms now come from the template via `create_room_bookings_from_template`). |
| ~~9.117~~ | ~~Event/Showing UI collapse (no model change)~~ | ✅ 2026-06-07 | Event Hub presents a single-occurrence event as one thing: heading "{Occurrence} & booking" + one inline card; the plural list + "Confirm all" only appear once a 2nd showing exists (`is_series` context flag). DB/code identifiers (`Showing`, `confirmed`) unchanged — status badges stay neutral "Confirmed/Unconfirmed". **Room un-booking fix:** new `Showing.occupies_rooms()` / `visible_room_bookings` — cancelled dates and rejected events now free their rooms on the diary + calendar (bookings kept for reversibility); applied in `edit_diary_data`, the diary list grouping, and the `showing_for_room_at`/`other_room_bookings_at` template tags. Deferred: a true "rota open but not public" state (needs a model field). Plan: `~/.claude/plans/golden-greeting-stonebraker.md`. |
| ~~9.118~~ | ~~Venue-configurable occurrence terminology (good-neighbours decoupling)~~ | ✅ 2026-06-07 | `SiteConfiguration.occurrence_noun` / `occurrence_noun_plural` / `confirm_label` (migration `diary/0066`), editable at runtime via the Panopticon site-config dashboard (new "Terminology" group). **Cinema-first defaults** ("showing"/"showings"/"Confirm") preserve The Cube's language on `master` (per SPEC §"Relationship with the Cube Microplex" — features configurable, not hardcoded); `seed_dev_data` sets S&S to "date"/"dates"/"Publish & open rota". Event Hub headings, badges' tooltips, buttons (incl. the dual publish+rota CTA), the New Event form's room-offset preview + field tooltips, and `update_showing_status`/`add_event`/add-showing flash messages all read from the config. **Edit Showing form** (`form_showing.html` + `ShowingForm`): heading "Booking @" → "{Occurrence} @"; the `confirmed`/`cancelled`/`hide_in_programme`/`sold_out`/`discounted` checkboxes moved out of the top into a crispy `Fieldset` "Status & visibility" below the date/time details (confirmed relabelled "Confirmed (public + rota open)"); "Delete Booking" → "Delete this {occurrence}"; JS confirm dialog, "Add another {occurrence}" link, and the historic-edit error all noun-driven. 9 new tests (incl. a config-override case); full suite 826 green. |
| ~~9.119~~ | ~~Edit Showing form layout polish + programming-queue seed data~~ | ✅ 2026-06-07 | **Layout** (`form_showing.html` + `ShowingForm`): the form ran Bootstrap's `form-horizontal` but the site is on Bootstrap 5 (no `.form-row`), so on a half-width viewport labels crammed into the 2-col strip and wrapped (pushing the ⓘ tooltip onto its own line) and the status checkboxes were oddly indented. Switched to a vertical layout (labels above fields); grouped the three call-times (setup/doors/final) into one responsive `row`; added a `Details` heading for parallelism with the Room booking / Rota / Rota notes sections. Bottom buttons now form one flex action bar (`Update` + `Add another` grouped, `Delete` pushed to the far end) — the bottom `Update` sits outside `<form>` and submits it via `form="showing-edit-form"`. (Also fixed a multi-line `{# #}` comment that was leaking into the HTML.) **Seed data:** new `_seed_programming_queue` in `seed_dev_data.py` adds 6 proposed/draft events 2–5 months out with a spread of metadata completeness — from a fully-detailed proposal (copy, terms, room) down to a bare name + one placeholder date; two carry multiple placeholder dates (with rooms held) a programmer would later cancel; one deliberately omits terms to exercise the publish gate; one omits a room. `created_at` backdated so the queue's oldest-first ordering is meaningful. Idempotent; 369 diary tests green. |
| ~~9.105~~ | ~~Configurable programme filter buttons~~ | ✅ 2026-06-03 | `EventTag.filter_group` nullable CharField + migration `diary/0058`; `PROGRAMME_FILTER_GROUPS` setting in `settings_common.py` (Film/Music/Cafe/Meeting by default). `edit_event_tags` formset gains `filter_group` dropdown (choices built from settings at render time). `_view_diary` passes `filter_groups` + `tag_filter_map_json` to template. S+S programme template renders button row + inline `window.PROG_TAG_FILTER_MAP`; `programme.js` extended with `cardMatchesGroup` + `updateFilterButtons`; AND-logic with text search; `?group=` URL param persistence. Seed data: film/music/cafe/meeting/performance/party/induction mapped. |
| ~~13.5~~ | ~~Collectives directory (CMS-managed)~~ | ~~Obviated 2026-05-25~~ | Covered by `Collective.public_copy` + `listed_publicly` + `/collectives/` view (9.68.1). No CMS page needed. |
| ~~**GDPR.1**~~ | ~~**Volunteer right to erasure (anonymisation)**~~ | ✅ 2026-05-05 | Panopticon-only "Anonymise this volunteer" action on volunteer edit page. Overwrites all PII on `Member`, `Volunteer`, and `User` records; blanks `RotaEntry.name` by case-insensitive text match; deletes `TrainingRecord` and `VolunteerEventMark`; creates `AnonymisationLog` entry (pk + performer, no PII). Confirmation step requires typing volunteer's name. `AnonymisationLog` model + migration `members/0011`. 12 tests. |
| ~~**WCAG.1**~~ | ~~**WCAG 2.1 AA structural accessibility pass**~~ | ✅ 2026-05-20 | Restored keyboard focus outlines (removed legacy `outline:none` in layout.css + static_pages.css). Added skip-to-main-content link, `<main>` landmark, `aria-label` on `<nav>`, `role="status" aria-live="polite"` on messages list, to both `base_public.html` templates. Converted mobile menu `<a>` → `<button>` with `aria-expanded`/`aria-controls`; JS syncs state. Added visually-hidden label to archive search input; unhid required-field asterisks in event form. Alt-text field widened to resizable Textarea. Added `::before` text prefixes to message types (colour-no-longer-sole-differentiator). `aria-label` on `[more]` links. Fixed `<main>` flex layout breakage. Wagtail article pages: removed `&nbsp;` spacer divs, restructured no-image/centred cases; removed `background-color:#fff` override so article cards inherit site colour scheme. |
| ~~Bug A~~ | ~~`/toolkit/` index page crashes~~ | ✅ 2026-02 | Fixed mailer URL namespace in `urls_flat.py` |
| ~~**Bug AD**~~ | ~~Mobile: collectives card view overflows viewport; navbar/burger disappear~~ | ✅ 2026-05-12 — `collectives.html`: `minmax(28rem, 1fr)` → `minmax(min(28rem, 100%), 1fr)` so cards can't exceed the viewport width. `base_admin.html`: added `overflow-x: clip` to `body` as a systemic catch-all (unlike `overflow-x: hidden`, `clip` doesn't break `position: fixed` elements — so the navbar stays put even if a future inner element overflows). |
| ~~Bug B~~ | ~~Wagtail `translation_key` column overflow~~ | ✅ 2026-02 | Migration `0013_widen_page_translation_key.py` — widened column to varchar(36) |
| ~~Bug C~~ | ~~Rota text shows raw HTML entities~~ | ✅ 2026-02-28 | `edit_rota.js`: `loaddata` callback decodes entities before populating the inline editor |
| ~~Bug D~~ | ~~Clearing rota slot over-eager email prompt~~ | ✅ 2026-02 | `ROTA_CLEAR_EMAIL_PROMPT_ENABLED` setting added |
| ~~Bug F~~ | ~~Time picker is a slider~~ | ✅ 2026-02-28 | `JQueryDateTimePicker` → `type="datetime-local"` + flatpickr 4.6.13; desktop gets calendar+time popup; mobile defers to native OS picker |
| ~~Bug G~~ | ~~Date/time picker clips behind navbar~~ | ✅ 2026-02 | Raised z-index in `edit_form.css` |
| ~~Bug H~~ | ~~Rota role icons orphan onto new line~~ | ✅ 2026-02-28 | `edit_rota.html`: wrap last word + badge in `white-space:nowrap` span |
| ~~Bug I~~ | ~~Sidebar nav overflows viewport~~ | ✅ 2026-02-26 | `#sidebar` `position:fixed; height:100vh; overflow-y:auto` + `#site-nav` `position:relative; top:auto` in `site_custom.css`; root cause: absolutely-positioned children don't contribute to scroll height |
| ~~Bug J~~ | ~~Calendar broken by jQuery 3.5 htmlPrefilter~~ | ✅ 2026-02-28 | `jQuery.htmlPrefilter = function(html) { return html; };` added in `edit_event_calendar_index.html` before `init_calendar_view`; restores pre-3.5 no-op so FullCalendar 3.x index-based seg correlation works; fixed on both `sns_2026_overhaul` (commit `e756658`) and `feature/event-edit-overhaul` (commit `c8f6ee1`) |
| ~~**Bug K**~~ | ~~Rota `&amp;` display glitch + security audit of loaddata decode~~ | ✅ 2026-03-02 | Fixed in `edit_rota.js`: use jeditable's `data` option (value-transform callback), not `loaddata` (which is POST params for `loadurl` AJAX — never fired here). Regex decode of Django's 6 escape sequences. Server returns unescaped plain text — do NOT call `escape()` there, browser re-encodes on innerHTML read. See TASKS.md Bug K for full jeditable encoding archaeology. |
| ~~**Bug L**~~ | ~~Wheelchair strikethrough too subtle~~ | N/A — model changed | Wheelchair is now an affirmative ♿ badge on roles confirmed accessible; the strikethrough model is gone. No further action needed. |
| ~~Bug W~~ | ~~209 test failures: `base_login.html` missing from main `templates/`~~ | ✅ 2026-03-06 | Created `templates/base_login.html`: minimal Roboto Mono shell with venue logo; no nav. Auth templates already extended it; it only existed in `star_and_shadow_templates/`. All 433 tests now pass. |
| ~~Bug X~~ | ~~static files stale after Docker rebuild~~ | ✅ 2026-03-21 | Root cause: `static/` (gitignored but on-disk) was copied into the image by `COPY . /site/`; `collectstatic` saw same timestamps on source + dest and skipped overwrite. Fix: `static/` added to `.dockerignore`; `collectstatic --clear` added to Dockerfile. |
| **Bug Y** | Rota meta row: time string and calendar icon misaligned on mobile | Deferred indefinitely | Root cause: `<details>/<summary>` baseline interacts oddly with mixed flex children. Attempted `align-items: baseline` / `align-self: center` — marginal improvement only. Not worth further time right now. |
| ~~settings cleanup~~ | ~~Settings deadweight + `general_training_enabled`~~ | ✅ 2026-05-31 | `general_training_enabled` added to `SiteConfiguration` (migration diary/0053); gates the GST nudges in volunteer list, profile Key dates row, training report, and the GST option in the `TrainingRecordForm`. When off, the GST prefetch is also skipped. Separately: removed all settings.py values that were dead at runtime (only used to seed the `SiteConfiguration` row in migration 0019): `MAILOUT_DETAILS_DAYS_AHEAD`, `MAILOUT_LISTINGS_DAYS_AHEAD`, `MAX_COUNT_PER_ROLE`, `ROTA_CLEAR_EMAIL_PROMPT_ENABLED`, `DEFAULT_TRAINING_EXPIRY_MONTHS`, `SHOW_ARCHIVE_IMAGES`, `IMAGES_START_DATE`, `FILMS_START_ON_TIME`, `ROTA_SHOW_TAGS` from both `settings_common.py` and `settings_ss.py`. `EVENTLINK_EXTRA_ALLOWED_DOMAINS` kept (still live as fallback in `validators.py`). Post-install GUI config table added to ONBOARDING.md. |
| ~~bug-i-seed-data~~ | ~~Bug I seed data~~ | ✅ 2026-02-26 | Six extra top-level nav pages added to `seed_dev_data` to reproduce sidebar overflow in dev |
| ~~bugs-txt-triage~~ | ~~BUGS.txt triage~~ | ✅ 2026-02-26 | Active items appended to TASKS.md |
| ~~calver-versioning-dev-waterm~~ | ~~CalVer versioning + dev watermark~~ | ✅ 2026-03-21 | `VERSION` file at repo root; `settings_common.py` reads it; `venue` context processor exposes `{{ VERSION }}` to all public templates; `base_public.html` watermark shows `DEV · {{ VERSION }}`; `.dockerignore` gains `static/` + `staticfiles/` entries; `collectstatic` uses `--clear` to prevent stale static assets. See ONBOARDING.md "Releases and Versioning". |
| ~~css-style-inconsistency-acro~~ | ~~CSS style inconsistency across public views~~ | ✅ 2026-02-26 | Extracted shared overrides into `site_custom.css`; removed `font-size: inherit` that made programme-page nav links too large; `programme_custom.css`, `static_pages_custom.css`, `event_custom.css` slimmed to page-specific rules only |
| ~~docker-dev-environment-s-s-s~~ | ~~Docker dev environment (S&S settings)~~ | ✅ 2026-02 |  |
| ~~emergency-suspension-safegua~~ | ~~Emergency suspension (safeguarding) — `Volunteer.STATUS_SUSPENDED`~~ | ✅ 2026-05-29 | Added a fourth volunteer status, **Suspended**, as the emergency-deactivation lever. `Volunteer.save()` syncs `user.is_active` on the suspend/reinstate transition (immediately blocks login and drops live sessions) and clears the volunteer's future rota entries (past entries preserved); reinstating restores login + rota visibility but **not** the cleared shifts. Migration `members/0019`. Removed the manual "Login enabled" (`is_active`) checkbox from `UserForm` so `status` is the single canonical login control — the two could otherwise drift. Suspension is Panopticon-only (choice hidden from non-superusers). Status options rewritten with always-visible plain-English consequences; suspended option visually set apart. Summary/list views now show a "Suspended" badge. SPEC §"Volunteer status, login access and suspension" rewritten, incl. guidance to prefer Anonymise over hard-delete for removing a suspended volunteer. 9 new tests (185→195 members suite green). |
| ~~event-copy-xss-sanitization~~ | ~~`Event.copy` XSS sanitization~~ | ✅ 2026-03-03 | `nh3` added to `requirements/base.txt`; `copy_html` property sanitizes via allowlist before `mark_safe`; strips `<script>`, event-handler attrs, `javascript:` hrefs; `star_and_shadow_templates/mailout_body.html` updated from `copy\ |
| ~~event-edit-workflow-overhaul~~ | ~~Event edit workflow overhaul (Phases A–C)~~ | ✅ 2026-02-28 | Phase A: rota notes in showing edit. Phase B: popup mode removed. Phase C: Event Hub (`edit-event-details-view`) — showing cards with confirm/cancel, add-showing form, completeness bar; `update_showing_status` endpoint; add_event/EditEventView/delete_showing redirect to hub; 380 tests pass |
| ~~event-hub-edit-form-lockstep~~ | ~~Event hub ↔ edit form lockstep~~ | ✅ 2026-05-29 | Event hub (`view_event_privatedetails.html`) was missing three `EditEventForm` fields: `trailer_url`, `age_restriction`, and the approval group (`approval_type` + `approved_at_meeting_date`/`meeting_name`/`meeting_minutes_url`). Added all to the details `<dl>` with matching ⓘ tooltips, so the hub view and edit form now show the same 21 fields. |
| ~~event-hub-surface-all-fields~~ | ~~Event Hub: surface all fields + tooltips~~ | ✅ 2026-02-28 | All event fields now always visible (with "—" fallback); `outside_hire`/`private` split into separate Yes/No rows; Bootstrap 4 tooltip ⓘ on every field label + action badges; Private/Confirm/Unconfirm tooltips explain consequences |
| ~~field-tooltips-on-event-edit~~ | ~~Field tooltips on event edit form~~ | ✅ 2026-03-06 | JS injects ⓘ icons onto crispy-rendered labels in `form_event.html`, matching the tooltip texts from the event hub view. No form class changes. |
| ~~fresh-install-migration-cras~~ | ~~Fresh install migration crash (`content.0013`)~~ | ✅ 2026-02-26 | Added `wagtailcore.0057` dependency; replaced `RunSQL` with safe `RunPython` (INFORMATION_SCHEMA check) |
| ~~grid-view-volunteer-banners-~~ | ~~Grid view volunteer banners not filling cells~~ | ✅ 2026-02-26 | `.showing .volunteer-badge` set to `display: block; width: 100%` |
| ~~homepage-list-view-layout-br~~ | ~~Homepage list view layout broken by volunteer event info~~ | ✅ 2026-02-26 | `.list .volunteer-badge` strips box styling to fit compact inline text |
| ~~live-word-counter-for-copy-s~~ | ~~Live word counter for `copy_summary`~~ | ✅ 2026-02 | Vanilla JS; 25-word target with colour coding |
| ~~onboarding-md-accuracy-pass~~ | ~~ONBOARDING.md accuracy pass~~ | ✅ 2026-02-26 | Linux Docker prereqs, venue clarity (S+S default/Cube opt-in), corrected settings table, removed BUGS.txt ref |
| ~~rota-event-links-event-hub~~ | ~~Rota event links → Event Hub~~ | ✅ 2026-02-28 | `edit_rota.html`: `edit-event-details` → `edit-event-details-view` so rota event-name links open the hub, not the bare edit form |
| ~~s-s-initial-bring-up-bugs~~ | ~~S+S initial bring-up bugs~~ | ✅ 2026-02 | POST-only logout, Font Awesome path, logo sizing, Bootstrap sourcemap, template caching |
| ~~seed-dev-data-management-com~~ | ~~`seed_dev_data` management command~~ | ✅ 2026-02 | 29 roles, 16 tags, 15 volunteers, 12 events |
| ~~seed-dev-data-rewrite-fix-8-~~ | ~~seed_dev_data rewrite — fix 8 bugs preventing seeding~~ | ✅ 2026-03-27 | Replaced external image downloads with local PIL placeholders; fixed indentation (rota/image/link creation was outside event loop); fixed `_seed_bulk_volunteers` (undefined `_`, tuple not unpacked, `return` inside loop); fixed monthly events nth-weekday calculation; fixed `_seed_one_recurring_showing` inner loop nesting; fixed `_seed_cms_pages` (tuple unpack, Wagtail tree API, `body=` field name, added missing CMS body constants); fixed `_seed_index_links` (field names `text`/`link`/`name`); created missing migration `diary/0018`; changed `--bulk-volunteers` default to 0 (opt-in). Seed now completes in ~4s with no network calls. |
| ~~staging-database-migration-t~~ | ~~Staging database migration to master~~ | ✅ 2026-03-31 | Successfully migrated staging DB from `s+s` branch to current `master`. Fixed migration name mismatches (`members.0008`/`0009` renumbering), added 19 new migrations, all 9,214 events / 10,994 showings / 2,334 members / 1,879 volunteers preserved. Created `migrate-staging-db.sh` automation script and `docs/DATABASE_MIGRATION.md` documentation. |
| ~~volunteer-pool-management-to~~ | ~~Volunteer pool-management tools + returning-volunteer experience~~ | ✅ 2026-05-29 | Day-based site settings (`volunteer_dormancy_days` 365, `volunteer_never_logged_in_grace_days` 90, `volunteer_purge_days` 1095) replacing `volunteer_dormancy_months`; data migration carries over any tuned value. `auto_dormancy` rewritten to set **Dormant** status (Active→Dormant only; never retires/deletes) and now catches never-logged-in accounts (the old `last_login__lt` query silently dropped NULLs). Dropped the redundant `login_inactive` flag (+ its view/URL/template) — `status` is the single source of truth. New panopticon **pool-health dashboard** (`/volunteers/view/pool-health/`) listing Dormant (with re-induction badges) and purge candidates. Returning dormant volunteers get a dashboard **welcome-back card** with one-click "I'm back" reactivation + induction nudge, and the rota force-enables the beginner-role highlight for them. Erasure logic extracted to `Volunteer.anonymise()`, shared by the web flow and a new guarded **`purge_stale_volunteers`** command (dry-run default; `--apply` + typed `"anonymise N volunteers"` phrase). 25 new tests; full members+diary suite (580) green. Built from `~/.claude/plans/let-s-create-tools-for-pure-mist.md`. |
| ~~volunteer-status-cleanup-dro~~ | ~~Volunteer status cleanup: drop `active`, retire dedicated workflow~~ | ✅ 2026-05-29 | `status` (active/dormant/retired) is now the single source of truth. Removed the redundant `active` BooleanField (migration `members/0018`); added `VolunteerQuerySet.active()/.inactive()` + an `is_active` property, and rewrote ~15 query/template/command call sites. Deleted the dedicated retire/unretire workflow (`select_volunteer`/`activate_volunteer` views, `select_volunteer.html`, 4 URLs, all nav links) — retirement is now done via the status radio on the profile page. The vols-admin notification email is folded into the profile save, firing whenever a volunteer joins/leaves the active roster. Fixed a latent bug in `keep_vols_from_csv_retire_everyone_else` (set `vol.active=False`, which `save()` immediately recomputed — now sets `status`). Tests updated; full suite (748) green. |

### 3b. Infrastructure / deployment hardening

Items identified during the April 2026 SNS production site outage investigation. The critical fixes (gunicorn workers, timeout, max-requests, DB connect_timeout, EMAIL_TIMEOUT, stop_grace_period) have already been applied. These are the remaining loose ends.

| # | Item | Size | Notes |
|---|------|------|-------|
| ~~**D.1**~~ | ~~**WhiteNoise cache-busting storage backend**~~ | ✅ 2026-04-30 | `STORAGES` dict added to `docker_settings_prod_ss.py` with `CompressedManifestStaticFilesStorage`; dry-run collectstatic verified clean (312 files, no errors). |
| ~~**D.2**~~ | ~~**Docker health checks**~~ | ✅ 2026-05-06 | `/health/` view in `toolkit/index/views.py` — calls `connection.ensure_connection()`, returns 200 `ok` or 503 `db unavailable`. Wired into `docker-compose-production.yml`: `toolkit` service uses HTTP check; `mailer` uses `pgrep -f mailerd`. Both: 30s interval, 3 retries, 60s/30s start_period. |
| ~~**D.3**~~ | ~~**Dependency pinning**~~ | ✅ 2026-05-29 | `uv pip compile requirements/docker.txt --python-version 3.11` → `requirements/docker.lock` (45 packages fully pinned). Dockerfile build stage now installs from lockfile. Full image build verified clean. |
| ~~**D.4**~~ | ~~**Docker resource limits**~~ | ✅ 2026-05-06 | `toolkit: 512m / 1.0 CPU`, `mailer: 192m / 0.5 CPU`. Production is 2GB total; leaves ~1.3GB for OS + MySQL. |
| ~~**D.5**~~ | ~~**Console email backend in dev settings**~~ | ✅ 2026-04-21 | `EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'` already in `devserver_settings.py` (line 55). |
| ~~**D.6**~~ | ~~**Screenshot automation for release demos**~~ | ✅ 2026-06-05 | `scripts/capture_screenshots.py`: Playwright-based script captures public + authenticated pages at 5 viewports (mobile-narrow 375×1100, mobile 390×1200, tablet 768×1024, desktop 1280×800, wide 1920×1080). Logs in as volunteer/programmer/panopticon; discovers event IDs dynamically; collapses rota filter box on mobile. Pixel-diff against baselines via Pillow. Generates static HTML gallery with CSS before/after sliders + "changed only" filter. Host-only (`uv run --with playwright --with Pillow`); no Docker changes. Screenshots stored in `screenshots/` (gitignored). |

---

### 4. Phase 3: What to build next

Priorities agreed 2026-05-25. Full specs in [TASKS.md](docs/TASKS.md).

**High priority — do these first:**
1. ~~**D.3 Dependency pinning**~~ — ✅ 2026-05-29
2. ~~**9.41 Calendar: clickable room filter in legend sidebar**~~ — ✅ already implemented (see row above)
3. **9.74 Permission model redesign** — go ahead; Panopticon-only PII; no collective discussion needed
4. **9.21 Recurring events / batch clone to dates** — handy feature, near top of list
5. **9.48 Template export/import** — MVP (copy-paste JSON round-trip)
6. **9.90 Access transparency: privileged user list** — high impact, low effort; gate behind Labs SiteSettings

**Medium priority:**
7. **9.58 Programme: one card per showing** — replaces stacked-dates model; bold date = this showing's next date
8. **9.89 Weekly volunteer digest email** — very useful for the effort; opt-in
9. **9.88 Shared shopping list** — self-contained Labs feature; bar stock excluded
10. **9.54 Structured event cost terms** — gate behind Labs SiteSettings; MVP = `cost_type` dropdown only
11. ~~**9.59 Focal point picker**~~ — obviated by 9.107 crop selector
12. **9.79 Tool library** — Labs section; gate behind SiteSettings

**Next agreed:**
- ~~**9.66 Film event metadata + TMDB integration**~~ — ✅ 2026-06-11

**Backlog (blocked or complex):**
- **9.102** ~~Replace Masonry.js with CSS Grid on programme index~~ — ✅ 2026-06-03 (done above)
- ~~**9.99 Volunteer stats page**~~ — ✅ 2026-06-11 — Personal "your history at S+S" page: activity heatmap (Unicode block chars, colour + char encoding for a11y), shifts/year bars, role breakdown grouped by `stats_label`, role evolution timeline, milestone shifts (archival framing, anti-gamification), training & qualifications, programming gate (configurable via SiteSettings). `stats_label` field on `Role`, `programming_min_event_shifts` + `stats_training_tag_slugs` on `SiteConfiguration`. Nav links in desktop + mobile navs. Panopticon view-as. 14 tests. 862 tests pass.
- ~~**9.96 Volunteer pool management GUI**~~ ✅ 2026-06-11 — Pool health page gains: auto-dormancy preview + apply GUI; one-click "Restore to active" on dormant/retired rows; `retention_exempt` + `retention_exempt_reason` fields on `Volunteer` (excluded from `purge_candidates()`; tier 4 section on pool health; editable in Panopticon profile); last-gasp re-engagement email per purge candidate (configurable subject/body/cooldown via SiteConfiguration; `LastGaspEmailLog` model enforces cooldown); active-membership guard on anonymise view + `purge_stale_volunteers` command (`--include-members` opt-in). Migrations `members/0025`, `diary/0079`. 17 new tests; 912 total green.
- **9.87 Collectives → simplelists sync** — high importance; defer until prod access available; warn about sync-state complexity (unsubscribes outside our interface)
- ~~**9.4 Induction tracking**~~ — ✅ 2026-06-11 — New `toolkit/inductions/` app: `InductionSession` (slug-based public sign-up URL, custom questions as JSON, session types), `InductionSignup` (per-attendee PII + custom responses, auto-purge), `InductionRequest` (1:1 access-needs queue), `InductionsSettings` singleton (per-email templates, welcome pack URL, purge days). Panopticon session management with live check-in (AJAX, confirmation prompt, inline volunteer account creation). Simplelists CSV export (first/last/email, no headers). 3-day reminder + auto-purge management commands. `send_induction_reminders` and `purge_induction_signups` commands. Feature toggle via `InductionsSettings.inductions_enabled`. Wired into `urls_flat.py` (S+S) and `urls.py` (Cube). 848 tests pass.
  - ✅ **URL ordering bug fixed 2026-06-11:** `<slug:slug>/` wildcard was listed before `manage/` patterns, so `/inductions/manage/` was caught as a slug lookup. Moved all `manage/` paths to the top of `urls.py`.
- **9.100 Role qualification gates** — training-gated rota sign-up (e.g. can't take Projectionist without the Projection induction) with shadow-2–3-times progression. First-draft spec in TASKS.md §9.100; needs design decisions (who logs a shadow; advisory vs blocking default; format-specificity) + collective buy-in. MVP (advisory only, no lock-out) is 🔵 S; full feature 🟡 M.
- ~~**9.101 Lost & found log**~~ ✅ 2026-05-31 — `FoundItem` model, sequential IDs (L-001 format), log form (any volunteer), tabbed list + detail/claim/dispose/printable-label views (panopticon), overdue flagging from `SiteConfiguration.lost_and_found_retain_days`. Photo included. Nav links in desktop + mobile menus.
- ~~**9.104 Programme RSS/Atom feed**~~ ✅ 2026-06-10. `BasicWhatsOnFeed` improved: 60-day lookahead (was 7), titles include date/time, descriptions use `copy_summary` first, `item_pubdate` from `showing.created_at`. RSS autodiscovery `<link rel="alternate">` moved to `base_public.html` (all public pages). Open Graph tags (`og:type`, `og:site_name`, `og:url`, `og:title`, `og:image`) added to `base_public.html` as site defaults; event pages override with event name, description, and media image via `{% block opengraph %}`.
- ~~**9.107 Letterbox bars on image upload**~~ — ✅ 2026-06-05. `bar_colour` field on `MediaItem`; on new upload with colour set, Pillow pads to `SiteConfiguration.thumbnail_crop_width/height` ratio (default 2:3). Colour picker + Canvas dominant-colour swatches in event edit form. Fixes latent `poster.py` bug (`thumbnail_crop_width/height` referenced but missing). Migration 0060.
- **9.63 Room availability overlay on map** — helpful but not a priority
- ~~**9.49 Runtime-configurable permissions**~~ ✅ 2026-06-15 — See table row above.
- ~~**9.124 Configurable access levels**~~ ✅ 2026-06-15 — Phase 1 (read-only table) shipped 2026-06-10. Phase 2 (editable dropdowns, `perm_*` fields, `feature_required` decorator, nav guards) shipped as part of 9.49.
- ~~**9.29 Role management UX (Phase 1)**~~ ✅ 2026-06-10. Added "Entries" column to roles table showing rota sign-up count per role. Inline rename warning appears when a role name is changed: "⚠ Renaming affects N existing rota entries." (JS, no round-trip). Fixed misleading "renaming is safe" alert text — now accurately states that a rename retroactively updates the name everywhere. Deeper design questions (multiple ad-hoc "other" roles, removing a role from a showing that has entries) remain open and need collective input before implementation.
- ~~**9.120 Volunteer CSV export: full page + PII notification**~~ ✅ 2026-06-10. Export page with five field-group checkboxes (basic always selected); personal-data groups (contact, address) badged as "personal data" with GDPR reminder; all exports logged to `ExportAuditLog` model; audit log view at `/volunteers/view/export/audit/`. Tests pass (219).
- ~~**9.121 Qualification report + training/qual page consolidation**~~ ✅ 2026-06-10. New `/volunteers/qualification-report/` showing all qualifications, holder counts, granted-on/by dates, and which roles gate on each qual (blocking/advisory badges). Role report retired — old URL redirects to qual report. New `/volunteers/bulk-record/` page merges training-group and bulk-qual-award UIs with type selector + inline explanation of the difference. Nav updated in desktop + mobile. Tests pass (219).
- ~~**9.122 Nav bar layout revision**~~ ✅ 2026-06-10 / follow-up 2026-06-20. Labs dropdown replaced by Community (Bulletins, Collectives, Volunteer directory, Jobs board, Community exchange, Building Map, Shopping list, Log found item, Lost & found — all `labs`-tagged). Website slimmed back to public-facing items. Members + Volunteers merged into People (superuser). Admin absorbed into Meta. Manage donations opened to Programmer+ (was superuser-only). Roles opened to Programmer+ — safe because `Role.delete()` archives rather than hard-deletes when rota entries exist. Mobile nav updated in parallel. Follow-up: removed stale "Panopticon-only page." alert from `form_edit_roles.html` (page is Programmer-tier, not superuser-only).
- ~~**9.123 Suspended volunteer: UX overhaul**~~ ✅ 2026-06-10. Option B implemented: Suspended removed from the status radio widget entirely (for all users); replaced with a separate Suspension card below the status section (red/amber border, consequence text, Suspend/Lift suspension button). New `toggle_volunteer_suspension` view + URL handles the action. Status help text rewritten to be accurate per-option.
- ~~**Test backlog: read-only role protection**~~ — ✅ 2026-06-10 `test_cant_unprotect_readonly_role` added to `RoleTests` in `test_models.py`.
- ~~**Test backlog: event booking logic**~~ — ✅ 2026-06-10 `s.confirmed == False` assertion added to `_test_add_event_common`; `test_create_as_confirmed_rejected_when_no_terms` added to `BatchAddShowingsTests` in `test_edit_misc.py`.
- ~~**9.120 extension — export reason**~~ ✅ 2026-06-12. Added optional "Export reason" field to `ExportAuditLog` and volunteer export form; reason displayed in audit log table. Migration `members/0026`.
- ~~**9.123 extension — suspension email**~~ ✅ 2026-06-12. Added `suspension_email_subject` and `suspension_email_body` configurable fields to `SiteConfiguration` (migration `diary/0082`). After any suspension (via suspension card or form), redirects to volunteer edit page with a preview panel showing rendered subject/body and a "Send suspension email" button (separate deliberate step). New `send_suspension_email` view + URL. Suspension message text updated (removed colon, added comma-dash). Migration `diary/0082`.
- ~~**Film API key in SiteConfiguration**~~ ✅ 2026-06-12 (TMDB); migrated to OMDb 2026-06-22. `SiteConfiguration.omdb_api_key` field (migration `diary/0083`). Site Settings form exposes it under "External APIs".
- ~~**Film section collapsible**~~ ✅ 2026-06-12. Film section card starts collapsed; auto-expands when event already has a film linked or has the "film" tag. Arrow rotation JS. `showLinked()` auto-expands on AJAX film link.
- ~~**TicketSource guidance text update**~~ ✅ 2026-06-12. Data migration `diary/0081` unconditionally replaces guidance with full step-by-step TicketSource setup guide (venue creation, seating plan, pricing categories, orphan seat rule, free ticket limits, dummy Nextcloud link).
- ~~**Notification visual language**~~ ✅ 2026-06-12. `bs_messages.html` redesigned as dismissible Bootstrap alerts with icons and left accent borders for warnings. `base_admin.html` now uses `bs_messages.html` everywhere (was `messages.html`). Removed duplicate include from `view_event_privatedetails.html`. 4 test assertions updated from `<li class="success">` to text-only form.
- ~~**Inductions: two-step check-in + account creation**~~ ✅ 2026-06-12. Checkboxes mark attendance (reversible AJAX, `manage_mark_attendance`); separate "Create accounts and send welcome emails" card is the irreversible step (`manage_create_accounts`). "Select all pending" button pre-ticks without creating. Inline panopticon edit modal per row (`manage_edit_signup`). Public session listing page (`session_list_public`).
- ~~**Site Settings → Inductions link**~~ ✅ 2026-06-12. Info alert with link to `/inductions/manage/settings/` added to bottom of `edit_site_configuration.html` when `InductionsSettings` exists.
- ~~**9.125 Keyboard shortcuts — Phase 2: global nav + rota power-user keys**~~ ✅ 2026-06-14. Extracted g-key nav mode from `edit_rota.html` into `keyboard-nav-admin.js` (global, all admin pages); exposes `window.AdminKeyboardNav.isNavMode()` for page-specific guard. Fixed g+o bug (nav mode had 1.5s auto-timeout; removed). Added sub-menu drill-down (g→w shows Website dropdown items with letter badges). Rota: J/{ forward one week, K/{ back one week; Ctrl+Enter saves rota notes; y/l copies permalink + shows "Link copied" toast; J/K reassigned from showing nav (was duplicate of j/k). Public sidebar g-key nav via `keyboard-nav-public.js` (jQuery only, no Bootstrap). Badge CSS: `.rota-nav-key-badge` (admin, amber) / `.pub-nav-key-badge` (public, amber). `data-nav-key` attributes on all admin navbar links + public sidebar links. Help modal updated. 427 tests green.
- ~~**9.125 Keyboard shortcuts — Phase 3: site-wide page shortcuts**~~ ✅ 2026-06-14. Diary edit list: `/` focus filter, Escape blur, `n` add event, `?` help modal, `{`/`}` month navigation, `F` fullscreen. Event hub: `e` edit event, `b` back, `r` rota, `j`/`k` scroll showing cards, `Enter` edit current card. Public programme: `j`/`k` navigate showings, `/` focus search, Escape blur, `o`/`Enter` open. Volunteer list: `j`/`k` navigate cards, `/` focus filter, Escape blur, `n` add volunteer. Programming queue: `j`/`k` navigate cards, `Enter`/`o` open event hub. Fixed Escape-not-blurring-empty-input bug in all filter inputs (was only blurring when filter had a value). 915 tests green.
- ~~**9.125 Keyboard shortcuts — Phase 5: command palette (Ctrl+K)**~~ ✅ 2026-06-14. `command-palette.js` (vanilla, no deps): Ctrl+K / Cmd+K opens a fuzzy-search modal over all admin commands. Multi-token substring search with label-match sorting. ARIA combobox pattern (role=combobox, listbox, option, aria-activedescendant). Arrow keys navigate, Enter activates, Escape/Tab/backdrop-click close. Commands grouped by category (Navigate, Website, Community, Meta, People, Actions) — permission-filtered via Django template conditionals. Shortcut hints shown next to each result as passive learning. `⌘K` navbar button for discoverability. `window.TKCommandPalette.open()` exposed for external callers. 915 tests green.
- ~~**9.125 Keyboard shortcuts — Phase 6: filter persistence + calendar shortcuts**~~ ✅ 2026-06-14. Diary edit list + volunteer list filter inputs now persist to `sessionStorage` across reloads (`diary-filter-text`, `vol-filter-text`). Escape clearing the filter also clears the stored value. Calendar page gains keyboard shortcuts via `calendar_index.js`: `{`/`}` prev/next period, `t` today, `n` new event, `/` focus name search. All shortcuts guard against input focus, g-key nav mode, and Ctrl/Alt/Meta modifiers. `?` help modal updated for calendar page. `aria-keyshortcuts` on calendar name search and "+ New booking" link. 677 diary+members tests green.
- ~~**9.125 Keyboard shortcuts — Phase 4: site-wide ? help modal + aria-keyshortcuts**~~ ✅ 2026-06-14. Extracted per-page `?` help modal into `keyboard-nav-admin.js` as a shared system: `AdminKeyboardNav.registerHelp(sections)` for pages to register shortcuts, `AdminKeyboardNav.suppressGlobalHelp()` for pages with their own `?` handler (rota). `?` now works on every admin page: shows "Global navigation" section always + page-specific sections. Diary list, event hub, volunteer list, programming queue updated. Rota keeps its own elaborate modal (suppresses global). Modal closes with Escape or ×. Added `aria-keyshortcuts` attributes to diary filter input and "+ New event" link; volunteer list filter and "add volunteer" link. 915 tests green.
- ~~**Bug AM — Access-level footer badge always showing "All volunteers" on write-required views**~~ ✅ 2026-06-22. `@permission_required("toolkit.write")` / `.read` never set `request.view_access_level`; template fell back to "All volunteers". Added `write_required`, `read_required`, `write_required_strict` decorators to `toolkit_auth/decorators.py` (the `_strict` variant raises 403 like the old `raise_exception=True` for labs). All 25 `@permission_required("toolkit.write/read")` uses in `edit_views.py`, `mailout_views.py`, `mailer/views.py`, `labs/views.py` replaced. 644 tests green.
- ~~**9.125 Keyboard shortcuts — Phase 7: feedback fixes**~~ ✅ 2026-06-22. Calendar: `{`/`}` → `[`/`]` (no shift key needed); added `m`/`w`/`3`/`R`/`M` view-mode shortcuts (month, week, 3-day, rooms-week, rooms-month); help modal updated. Diary list: added `j`/`k` event row navigation (respects filter, outline highlight, Enter/o opens); `scroll-margin-top` on month wrappers so month heading is not hidden behind navbar after `{`/`}` scroll. Volunteer summary page: added `j`/`k`/Enter/`o`/`n` keyboard shortcuts matching the profiles page. Rota remove-button UX + deletion warning specced as 9.142 in backlog.
- ~~**One-shot role description**~~ ✅ 2026-06-22. `_get_oneshot_roles_for_showing` now returns `description` field. `_parse_oneshot_roles` accepts `oneshot_desc_N` POST field and persists it on create/update. `form_showing.html`: description input added to new and existing one-shot role rows. No migration needed (`Role.description` already existed).
- **9.142 One-shot role: remove-button UX + deletion warning** — added 2026-06-22. Spec in `docs/tasks/events-and-rota.md`.
- **9.130 Printed programme archive: season model + gallery view** — added 2026-06-20 (user feedback: month-per-record doesn't suit S+S's seasonal publishing cadence; no public gallery exists). Spec in `docs/tasks/public-site.md`.
- **9.131 Clone from past events** — added 2026-06-21 (user feedback: clone picker only shows future events; programmers frequently repeat past screenings). Spec in `docs/tasks/events-and-rota.md`.
- **9.132 Create event template from an existing event** — added 2026-06-21 (user feedback: no way to save an existing event's setup as a reusable template; want to import all config and review before saving). Spec in `docs/tasks/events-and-rota.md`. Related: 9.131.
- ~~**9.133 Day-of-week colour motifs on programme cards**~~ ✅ 2026-06-22. `data-dow="{{ showing.start|date:"w" }}"` on each `.showing` card in `view_showing_index.html`. 3px left-border stripe per day (Mon violet → Tue blue → Wed green → Thu amber → Fri orange → Sat red → Sun pink) in `programme.css`. Day-of-week legend strip (`.dow-legend`) added above the programme grid with colour swatches. No model/view changes.
- **9.134 "More filters" panel for the programme** — added 2026-06-21 (user feedback: day-of-week checkboxes + count badge + "more filters" button beside existing filter row). Spec in `docs/tasks/public-site.md`. Related: 9.105, 9.133.
- ~~**9.135 Event hub: original image + thumbnail preview side-by-side**~~ ✅ 2026-06-22. Image row in `view_event_privatedetails.html` now shows two labelled images: "Original" (`editpreview` alias) and "Card thumbnail" (`indexview` alias, max-width 120px). No model/view changes.
- ~~**9.136 Bubble top-5 most-used tags to top of tag picker**~~ ✅ 2026-06-22. `EditEventView` annotates `EventTag` with `Count("event")` and passes `top_5_tag_pks_json` to context. JS in `form_event.html` post-renders the `.tag-pill-select` into two groups ("Common tags" / "All tags") with a heading + divider. CSS in `edit_form.css` for `.tag-pill-group-heading` and `.tag-pill-divider`.
- ~~**Inductions: welcome email + password link improvements**~~ ✅ 2026-06-23. `PASSWORD_RESET_TIMEOUT` extended from 3 days to 7 days. Default welcome email now includes `{username}` and a note pointing to the password reset form if the link expires. `{username}` and `{password_reset_url}` added as template variables (pills in settings UI, sample data for preview). `access_needs_enabled` BooleanField added to `InductionsSettings` (default `True`); when unchecked, the 1:1 request page returns 404. Migration 0006.
- ~~**Inductions 9.4 / 9.142 feedback pass**~~ ✅ 2026-06-23. Comprehensive testing feedback from `testing/B-induction-tracking-94.md`. **Bugs fixed:** slug no longer appends duplicate year when title already contains it; `access_needs_signup.html` broken link (`manage_session_list` → `session_list_public`); no-show confirmation dialog removed (toggleable UX makes it redundant); Undo button changed to grey (`text-warning` → `text-muted`); volunteer summary nav links removed (duplicated in nav); access_needs_thanks now shows fallback contact email (from `organiser_notification_email`); Inductions settings link added to Meta dropdown in `base_admin.html`; JS null-reference errors on empty-signup sessions (guarding `select-all-btn`, `create-accounts-btn`, `signup-table` — likely root cause of navbar dropdown bug). **New settings fields:** `privacy_policy_url` (linked from GDPR consent checkbox), `access_needs_intro_text` (configurable intro on 1:1 form). Migration 0005. **Signup form:** first name / last name split; GDPR consent checkbox with `privacy_policy_url` link and purge-days info; rota name preview JS. **Username format:** `JohnDoe` not `john-doe`; `preview_username` property on `InductionSignup`; JS `_slugPreview()` updated. **Spot registration:** `manage_add_walkin` view + URL; `<details>` walk-in form on session detail; AJAX row append + total count increment. **Duplicate account detection (go-live blocker):** `_find_existing_volunteer()` helper; `manage_create_accounts` pre-checks email then name match before creating; `duplicates` list in JSON response; per-duplicate resolution UI (send login link to existing account / create new account anyway); `manage_link_existing` view + URL; `_send_password_reset_email()` helper. **Cap + remove:** capacity displayed in live count header when set; × remove button on pending rows (Panopticon only, including walk-in rows); `manage_remove_signup` view + URL + JS handler. **Management commands:** `--dry-run` flag added to both `send_induction_reminders` and `purge_induction_signups`.
- **9.142 Induction session: attendee cap + signup removal** — added 2026-06-21 (per-session cap with site default; public page closes when full; if cap lowered, show over-cap warning and order by signup time; remove button for pending signups; inductors may exceed site default). Spec in `docs/tasks/volunteers.md`. Related: 9.4.
- **9.137 Bulk training: checkbox table + name search** — added 2026-06-21 (user feedback: shift-click multi-select fragile at 1000+ volunteers; want checkbox table + name filter matching qualification section). Spec in `docs/tasks/volunteers.md`. Related: 9.138.
- **9.138 Bulk training: optional simultaneous qual grant** — added 2026-06-21 (user feedback: training session and qual grant are two separate operations today; want optional "also grant qualification" dropdown on the training form). Spec in `docs/tasks/volunteers.md`. Related: 9.137.
- **9.139 Volunteer export: filter by upcoming shift or specific event** — added 2026-06-21 (use case: need to email all attendees when an event changes; want to export only volunteers signed up to a specific event or any upcoming shift). Spec in `docs/tasks/volunteers.md`. Related: 9.120.
- ~~**9.146 Shopping list: buyer-oriented view + first-class suppliers**~~ ✅ 2026-06-26. First-class `Supplier` model (`name`, `fulfilment_type`, `ordering_url/notes`, `account_holder`, `active`). `SupplierRecord` gains nullable FK to `Supplier`; data migration folds existing `supplier_name` strings into `Supplier` rows. `ProcurementPledge` gains `status` (pledged/ordered/oos/fulfilled) + nullable `intended_supplier` FK + `status_notes`; `fulfilled_at` stays authoritative. Three new views: supplier picker (`/shopping/buy/`), per-supplier shop sheet (`/shopping/buy/<id>/`), enrichment workbench (`/shopping/buy/unsorted/`). New POST handlers: `shopping_item_enrich` (attach supplier to item, `get_or_create` on new names), `shopping_buy_add` (add to order), `shopping_out_of_stock` (marks OOS, leaves need open). Nav: "Do a shop" added to Community dropdown + command palette. Admin: `Supplier` registered. Seed: SUMA (delivery) + Bookers (pickup) with linked SupplierRecords. 959 tests passing.
- ~~**9.126 Dashboard preferences to DB + favourite links panel**~~ ✅ 2026-06-16. (a) `dashboard_hidden_cards`, `dashboard_card_order`, `diary_days_ahead`, `favourite_link_keys` JSONField/IntegerField on `Volunteer` (migration `members/0027`). (b) Dashboard card visibility + order now persisted to DB via new `/toolkit/dashboard/prefs/` AJAX endpoint (`dashboard_prefs` view); JS uses AJAX POST for volunteers, localStorage fallback for non-volunteers; server renders initial hidden state so no flash of wrong content on load. (c) `diary_days_ahead` migrated from session to DB via updated `edit_prefs.py` (volunteer kwarg threaded through `edit_diary_list` and `set_edit_preferences`). (d) New "Quick links" dashboard card showing pinned favourites; `/toolkit/favourites/` manage page with access-level-filtered link catalogue (19 links, max 8 pinned). 916 tests green.

## Agent instructions

**Starting a task?**
1. Pick from "Immediate blockers" or "Phase 1" in order above
2. See [TASKS.md](docs/TASKS.md) for design rationale
3. See [CLAUDE.md](CLAUDE.md) for setup/context

**Just finished a task?**
1. Mark it ✅ + date in its table above
2. That's it — no other files need updating

**Parallelizing work?**
- Blockers + Phase 1 items are dependent (do in order)
- Phase 2 quick wins are independent (start any)

---

## Size legend

| 🟢 | 🔵 | 🟡 | 🟠 | 🔴 |
|----|----|----|----|-----|
| 1–4h | 4–16h | 16–40h | 40–80h | 80–160h |

---

*Navigation: [TASKS.md](docs/TASKS.md) · [SPEC.md](docs/SPEC.md) · [CLAUDE.md](CLAUDE.md)*
