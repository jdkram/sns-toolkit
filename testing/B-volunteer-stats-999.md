# Volunteer stats page (9.99) — unreleased

- [ ] Stats page loads at `/volunteers/stats/` when logged in as a volunteer
  RESULT:

- [ ] Activity heatmap renders; Unicode block characters visible; colour and character both encode activity level (a11y)
  RESULT:

- [ ] Shifts/year bars show plausible counts matched to the seeded data
  RESULT:

- [ ] Role breakdown groups correctly by `stats_label` (set on roles via the roles page); falls back to role name when no label set
  RESULT:

- [ ] Panopticon can view another volunteer's stats via `/volunteers/<pk>/stats/`
  RESULT:

- [ ] `stats_training_tag_slugs` in Site Settings controls which training tags appear in the Training & Qualifications section
  RESULT:

- [ ] `programming_min_event_shifts` threshold controls whether the programming gate shows as unlocked
  RESULT:
